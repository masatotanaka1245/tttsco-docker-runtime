# TODO.md

このファイルは、このアプリ共同開発における進捗管理表です。

## ステータス定義

- `未着手`: まだ着手していない
- `進行中`: いま実装を進めている
- `検証中`: 実装は終わり、ログや動作確認をしている
- `実装済み（要改善）`: 動く状態までは入っているが、品質やUXに改善余地がある
- `保留`: 現行構成から外しており、再導入方針が決まるまで止める
- `要確認`: 実装有無や優先度を追加確認したい
- `完了`: 実装と確認が終わっている

## 運用ルール

- `進行中` のタスクは常に 1 件だけにする
- 実装済みのものを `未着手` のまま残さない
- 未確認のものは断定せず `要確認` に置く
- 仕様変更を伴う場合は、`README_01.md` と必要に応じて関連ドキュメントも更新する
- 新しい再発防止ルールが出たら、`AGENTS.md` に追記する

## 進行中

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P1 | 進行中 | 質問分解 packet / `chat_reasoning_steps` 保存 / `project_meta` 自動 TODO 反映 / 圧縮 thread context の実運用データ確認と `save_worthy`・履歴圧縮ルールの微調整 | `AI_System_Data/public/api/chat.php`, `AI_System_Data/src/ChatQuestionDecomposer.php`, `AI_System_Data/src/AdvancedReasoningStepRecorder.php`, `AI_System_Data/src/ProjectMemoryAutoUpdater.php`, `AI_System_Data/src/ProjectTaskStateReducer.php`, `AI_System_Data/src/PromptManager.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/src/AdvancedRouteFinalizer.php`, `AI_System_Data/logs/chat_debug.log` | 実運用データを使った複合依頼で、`[QUESTION-DECOMPOSE]` / `chat_reasoning_steps` 保存 / `todo_source=decomposition` / `ai_project_todo_auto_md` の `進行中` / `未着手` が自然に維持されること、`status_alignment` 相談で `[PROJECT-MEMORY-AUTO] skipped=route_guard` が出て既存 TODO を潰さないこと、`[PROMPT-HISTORY]` / `[PROMPT-CONTEXT]` が route と最終回答を壊さないことを確認する |

## 未着手タスク

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P2 | 未着手 | `資料` タブまわりのフロント実装を `support.js` から段階的に分離し、`materials.js` 相当の専用モジュールへ整理する | `AI_System_Data/public/assets/js/support.js`, `AI_System_Data/public/assets/js/modules/`, `AI_System_Data/public/support.php`, `AI_System_Data/public/templates/modals.php` | 資料一覧、編集、保存後即時更新、削除後更新、AI回答からの追記が従来どおり動くことを確認する |
| P2 | 未着手 | `Datetime` / `時間帯` 系の質問を日別・月別ではなく時間帯別の粒度として解釈し、follow-up でも列・粒度継承を安定化する | `AI_System_Data/src/CsvAggregationPlanner.php`, `AI_System_Data/src/CsvDateAggregationRunner.php`, `AI_System_Data/src/ChatHistoryContextResolver.php` | `Datetimeから多い時間帯を教えて` と `時間帯ごとにグラフ化してください。` が時間帯粒度で返ることを確認する |
| P3 | 未着手 | headless browser による報告書 PDF 変換ログの文字化けを抑え、Chrome / Edge 実行可否が読み取れる形に整える | `AI_System_Data/src/ReportGenerator.php`, `AI_System_Data/logs/chat_debug.log` | `[REPORT] headless browser version:` 周辺のログが文字化けせず読めることを確認する |

## 実装済み（要改善）

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P1 | 実装済み（要改善） | CSV AI categorization を進捗付き非同期ジョブで実行し、結果を新しい CSV として生成する基盤 | `AI_System_Data/src/CsvAiCategorizationJobService.php`, `AI_System_Data/public/api/start_csv_ai_categorize_job.php`, `AI_System_Data/public/api/get_csv_ai_job_status.php`, `AI_System_Data/bin/run_csv_ai_categorize_job.php`, `AI_System_Data/public/assets/js/modules/csv.js`, `AI_System_Data/public/templates/modals.php` | ジョブ起動、進捗取得、完了後の新規CSV追加までは実装済み。残りはトーストの `ジョブが見つかりません`、完了後自動反映、UX文言、精度改善を確認・補正する |
| P1 | 実装済み（要改善） | 質問を因数分解して `chat_reasoning_steps` と `project_meta` の自動 TODO に反映し、長い履歴は圧縮して prompt へ渡す第1〜第8段 | `AI_System_Data/public/api/chat.php`, `AI_System_Data/src/ChatQuestionDecomposer.php`, `AI_System_Data/src/AdvancedReasoningStepRecorder.php`, `AI_System_Data/src/ProjectMemoryAutoUpdater.php`, `AI_System_Data/src/ProjectTaskStateReducer.php`, `AI_System_Data/src/PromptManager.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/src/AdvancedRouteFinalizer.php`, `AI_System_Data/src/ConversationIntentInterpreter.php`, `README_01.md`, `AGENTS.md` | `de2202c docs: document decomposition and auto-todo context strategy` で仕様と運用ルールを追記し、`8393100 feat(decompose): add entry question decomposition packet` で入口の質問分解 packet と `[QUESTION-DECOMPOSE]` / `chat_reasoning_steps` 保存を追加した。`8edcb68 improve(project-memory): derive auto todo from decomposed tasks` では `ProjectTaskStateReducer.php` を追加し、`save_worthy=false` を除外しつつ `進行中` 1件に絞る durable TODO reducer を導入した。`290b7fe feat(project-memory): wire decomposed tasks into auto todo refresh` では `chat.php` から `ProjectMemoryAutoUpdater::refresh(..., ['decomposed_tasks' => ...])` へ接続し、`[PROJECT-TASK-REDUCER] source=decomposition | current=1 | pending=1 | skipped=1`、`[PROJECT-MEMORY-AUTO] ... | todo_source=decomposition`、`task_after=履歴を要約する`、`next_after=報告書化する` を transaction rollback 付き確認で確認した。`6f0081d improve(prompt): strengthen compressed thread context` では `PromptManager::compactHistoryRows(...)` と `[PROMPT-HISTORY] rawTurns=... | compactTurns=... | rawChars=... | compactChars=...`、`[PROMPT-CONTEXT] history_layer=thread_summary` を追加し、factorizer 用 raw 履歴 3 件と summary 用圧縮履歴 8 件窓を分離した。さらに `2992889 fix(project-memory): keep short actionable tasks in decomposition todo` と `79fccf7 fix(decomposition): stabilize auto todo from decomposed tasks` で、日本語タスク正規化の UTF-8 破損修正、`報告書にしてください -> 報告書にする` などの TODO 向け正規化、`normal_rag.project_memory_consultation` の `status_alignment` を read-only にして `[PROJECT-MEMORY-AUTO] skipped=route_guard | ... | reason=consultation_read_only` を出す保護、`AdvancedRouteFinalizer.php` から decomposition session を再読込して advanced 完了後も `todo_source=decomposition` を維持する補強を加えた。`c7be3e3 improve(task-log): log save-worthy decisions for auto todo` では `save_worthy` の理由を `[TASK-REDUCER]` / `[PROJECT-MEMORY-AUTO] skipped=task_guard` で追えるようにした。`e9c7fe8 improve(decomposition): classify request mode for task updates` では decomposition packet に `request_mode` / `update_policy` を optional 追加し、route は「どこで処理するか」、`request_mode / update_policy` は「TODO / project_meta を更新してよいか」を判定する土台として分離した。分類は `consultation / command / artifact / clarification / ephemeral / unknown` と `read_only / todo_candidate_allowed / todo_candidate_denied / clarification_required / ephemeral_only`。`ProjectTaskStateReducer.php` は `update_policy` を TODO候補化の前段 guard として読み、`[TASK-REDUCER]` ログにも `request_mode / update_policy` を追加した。class-level 確認では、A. `履歴を要約して、報告書にしてください。` は `履歴を要約する -> request_mode=command / update_policy=todo_candidate_allowed`、`報告書にする -> request_mode=artifact / update_policy=todo_candidate_allowed` で TODO 結果 `進行中=履歴を要約する / 未着手=報告書にする` を維持。B. `この案件の現在の進行中タスクは何ですか？次に何をすべきですか？` は各 sub task が `request_mode=consultation / update_policy=read_only` となり reducer 結果は `state=null`。C. `3月分を見たいです` は `request_mode=clarification / update_policy=clarification_required` で TODO候補にしない。D. `前提を確認する` は `request_mode=ephemeral / update_policy=ephemeral_only` で TODO候補にしない。E. `この会話を報告書にしてください` は `request_mode=artifact / update_policy=todo_candidate_allowed` で TODO候補として通過する。確認済みコマンドは `git diff --check -- AI_System_Data/src/ChatQuestionDecomposer.php AI_System_Data/src/ProjectTaskStateReducer.php` OK、Docker 内 `php -l` で両ファイル OK、class-level A〜E 確認済み。`8918a7d improve(decomposition): refine request mode boundary rules` では `ChatQuestionDecomposer.php` だけを最小修正し、`3月分` を含む質問でも `売上` や `月別に集計` など明示集計語がある場合は `command / todo_candidate_allowed` に寄せるよう調整した。相談系語彙を追加し、`次はどこから手を付けるべきですか？`、`今の実装方針に問題はありますか？`、`このCSVをどう分析すればよいですか？` を `consultation / read_only` に寄せた。裸の曖昧命令 `このデータを見てください`、`集計してください` は `clarification / clarification_required` に寄せ、内部整理寄りの `一旦内容を整理する` は `ephemeral / ephemeral_only` に寄せた。一方で `ログを確認して、原因を特定してください。` は `command / todo_candidate_allowed` を維持した。境界確認では、維持ケースとして `履歴を要約して、報告書にしてください。`、`このCSVを分析して、問題点を整理してください。`、`このCSVを分析してください。`、`この会話を報告書にしてください`、`3月分を見たいです`、`前提を確認する`、`ログを確認して、原因を特定してください。` を確認し、既存成功ケースへの影響がないことを確認した。修正ケースとして `3月分の売上を月別に集計してください。 -> command / todo_candidate_allowed`、`次はどこから手を付けるべきですか？ -> consultation / read_only`、`今の実装方針に問題はありますか？ -> consultation / read_only`、`このCSVをどう分析すればよいですか？ -> consultation / read_only`、`このデータを見てください -> clarification / clarification_required`、`集計してください -> clarification / clarification_required`、`一旦内容を整理する -> ephemeral / ephemeral_only` を class-level で確認した。`6ac8eb7 improve(decomposition): treat analysis advice questions as read-only` では、分析方法や着手順を尋ねる相談文型を `consultation / read_only` に寄せた。成果物依頼を誤って consultation に落とさないため `artifact` 判定を `consultation` より優先し、狭い相談文型を `looksLikeConsultationAdviceRequest()` に分離した。`どのように分析したらよいでしょうか。`、`この案件で、まずどこから見ればよいですか。`、`何から見ればよいですか？`、`どこから分析すればよいですか？`、`まず何を確認すべきですか？`、`分析の進め方を教えてください`、`どのような観点で見ればよいですか？` は `request_mode=consultation`、`update_policy=read_only`、`reducer state=null` となり auto TODO へ上がらないことを確認した。一方で `このCSVを分析してください。`、`このCSVを分析して、問題点を整理してください。`、`ログを確認して、原因を特定してください。`、`問題点を分析して、対応方針を作ってください。`、`今年の売上を月別に集計してください。`、`分析の進め方を報告書にまとめてください。` は `command` または `artifact` として `todo_candidate_allowed` を維持した。`09353f9 fix(task-reducer): avoid dropping explicit detail requests as clarification` では `ProjectTaskStateReducer::analyzeEphemeralTask()` が `教えてください` を含む文を広く `clarification_request` としていたため、decomposer 側で `command / todo_candidate_allowed` になった明示作業依頼まで reducer で落ちていた問題を修正した。`looksLikeExplicitDetailWorkRequest()` を追加し、`CSV / 資料 / ログ / 売上 / 一覧表 / データ / 件数 / 月別 / 日別 / 違い / 問題点 / 結果` のような具体対象と、`集計 / 分析 / 比較 / 要約 / 抽出 / 整理 / 作成 / 出力 / 確認 / 特定 / まとめ` のような明示操作が揃う場合だけ、`教えてください` を含んでも clarification として落とさないようにした。`CSVデータを集計して詳細を教えてください。`、`この資料を要約して内容を教えてください。`、`ログを比較して違いを教えてください。`、`売上データを分析して問題点を教えてください。`、`出荷一覧表を月別に集計して結果を教えてください。`、`このCSVから件数を集計して教えてください。` は reducer で採用され `state != null` を確認した。`詳細を教えてください。`、`内容を教えてください。`、`これについて教えてください。`、`どれを見ればよいですか？`、`どのCSVを使えばよいですか？`、`集計してください。`、`このデータを見てください。` は引き続き TODO 化しないこと、`consultation / clarification / ephemeral` の既存 guard を維持していることも class-level で確認した。`f0e158e fix(decomposition): normalize dasu task wording` では `ChatQuestionDecomposer::normalizeSubQuery()` の汎用変換 `してください -> する` が先に適用されることで `出してください -> 出する` になっていたため、汎用ルールより前に狭い正規化 `出してください -> 出す` を追加した。`request_mode / update_policy / save_worthy / TODO採否`、分解粒度、外部依存は変更していない。文面確認では `次アクションを出してください -> 次アクションを出す`、`問題点を出してください -> 問題点を出す`、`件数を出してください -> 件数を出す`、`結果を出してください -> 結果を出す`、`この資料を要約して、次アクションを出してください。 -> この資料を要約して、次アクションを出す` を確認した。既存正規化も `履歴を要約してください -> 履歴を要約する`、`報告書にしてください -> 報告書にする`、`次アクションを決めてください -> 次アクションを決める`、`問題点を整理してください -> 問題点を整理する`、`CSVを分析してください -> CSVを分析する`、`ログを確認してください -> ログを確認する` を維持し、複合依頼 `履歴を要約して、報告書にしてください。` でも `進行中=履歴を要約する / 未着手=報告書にする` を維持した。`07a2067 improve(chat): log conversation intent profile` では `ConversationIntentInterpreter.php` を追加し、毎回のユーザー発話に対して `conversation_intent_profile` を生成して `chat_debug.log` へ `[CONVERSATION-INTENT]` を出す第1段を実装した。今回は既存 route / reducer / guard / DB 保存挙動は変えず、`conversation_relation`、`request_type`、`user_intent`、`expected_response`、`needs_todo`、`todo_policy_hint`、`answer_strategy` を観測可能にした。`5aa0311 fix(chat): enforce project scope in reasoning context` では `chat_reasoning_steps` の `session_id` 単独参照に `project_id` filter を追加し、対象は `AdvancedRouteFinalizer.php`、`chat_history_summary.php`、`ReportGenerator.php`。通常回答 / analysis / advanced の prompt には current project scope instruction を追加し、現在の `project_id` 以外の `CSV / PDF / documents / project_meta / chat_history / reasoning steps` を根拠にしない方針を明示した。あわせて `[PROJECT-SCOPE]` / `[PROJECT-SCOPE-WARN]` ログを追加し、`thread_id mismatch` は第1段では reject せず warning + safe fallback を維持した。`CSV / RAG / documents / project_meta` 検索本体は大改修せず、既存の project-scoped 状態を維持し、DB schema 変更・DB 更新は入れていない。軽量分類は `consultation / status_check / artifact / aggregate_data / follow_up / rollback / correction` を対象とし、class-level では `この方針は自然ですか？`、`回答精度を上げたいです。`、`チャットのキャッチボールがうまくできていない気がします。`、`質問の意図は常に解釈するようにしたいです。` が `consultation / proposal / needs_todo=0 / todo_policy_hint=read_only`、`現在の進行中タスクは何ですか？次に何をすべきですか？` が `status_check / consultation / status_answer / context_dependency=high`、`csv化してください` と `この内容を報告書にしてください。` が `artifact / create_artifact / needs_todo=1 / allow`、`このCSVを集計して詳細を教えてください。` が `command / aggregate_data / execution_result`、`はい、それでお願いします。` が `follow_up / command / context_dependency=high / needs_todo=0`、`やっぱり前の話に戻りたいです。` と `それは違います。質問の因数分解に戻りたいです。` が `rollback` / `correction` として観測できることを確認した。fixture project は削除済みで、`projects / chat_threads / chat_history / chat_reasoning_steps / project_meta / project_csv_files / documents` の残件 0、`chat_debug.log` restore 済み、`git status --short` も clean を確認済み。残りは advisory 相談語彙の不足・過剰マッチを実運用ログで継続確認すること、`教えてください` を含む別文型が広く救済されすぎていないか確認すること、`この資料の要点を教えてください。` の `request_mode=unknown` 改善余地、`csv化してください` の `artifact` 分類改善余地、`資料メモを開く` が current になる分解粒度の改善、複合依頼の 1 task 化改善、`ConversationIntentInterpreter` の `answer_strategy` を回答生成へ渡す接続、`ProjectMemoryAutoUpdater` の consultation / status_check / follow-up 系 auto TODO overcapture 抑止への接続、`source_project_ids` を `CSV / documents / doc_chunks / chat_history` など全 source で統一ログ化すること、`thread mismatch` を warning のまま維持するか将来 reject に上げるか判断すること、advanced 最終統合経路で prompt scope log が複数出る場合の微調整、global mode の軽い fixture / regression 確認、full source `project_id` guard の検討、実運用データでの継続確認、`unknown` を今後 `todo_candidate_denied` 寄りにするか慎重に検討すること、`chat_reasoning_steps` への metadata 永続化は別途検討すること、履歴圧縮の構造化は別TODOで扱うこと、`ProjectMemoryAutoUpdater` の `task_guard` ログに policy 集約情報を載せるか検討すること、`chat_analysis.php` / `chat_advanced.php` 側の履歴圧縮 helper の適用範囲見直し、TODO 文面の粒度と `save_worthy` 判定の実運用調整、必要に応じた live fixture 確認を次段で実施すること。なお `件数を出してください`、`結果を出してください` は文面だけ自然化し、既存の `too_short` による `state=null` は維持している。 |
| P1 | 実装済み（要改善） | `conversation_intent_profile` を `ProjectMemoryAutoUpdater` と `ChatRouteSelector` に接続し、consultation / status_check / rollback / correction / read-only follow-up 系の auto TODO overcapture を抑止しつつ、auto memory refresh / route selection decision を統一ログで観測できるようにする第1〜第5段 | `AI_System_Data/public/api/chat.php`, `AI_System_Data/src/ChatRouteDispatcher.php`, `AI_System_Data/src/ChatRouteSelector.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/public/api/chat_advanced.php`, `AI_System_Data/public/api/chat_history_summary.php`, `AI_System_Data/src/AdvancedRouteFinalizer.php`, `AI_System_Data/src/ConversationIntentInterpreter.php`, `AI_System_Data/src/ProjectMemoryAutoUpdater.php`, `AI_System_Data/src/PromptManager.php` | `ec4f8e0 fix(chat): skip auto todo for read-only intents` で `conversation_intent_profile` を `ProjectMemoryAutoUpdater` に接続し、`chat.php` から `ChatRouteDispatcher`、normal / analysis / advanced / history summary route、`AdvancedRouteFinalizer` 経由で profile を引き回すようにした。`consultation / status_check / rollback / correction / read-only follow_up` 系では snapshot 起点 generic TODO の durable 化を抑止し、`ProjectMemoryAutoUpdater` に intent-based skip 判定を追加した。ログでは `[PROJECT-MEMORY-AUTO-SKIP]` によって skip reason を観測可能にし、class-level では `command / artifact / aggregate_data` 系が従来どおり skip されないことを確認した。第1段では auto TODO だけでなく auto memory refresh 全体を skip している。続く `94504ab improve(chat): log auto memory refresh decisions` では `ProjectMemoryAutoUpdater` に統一 decision log helper を追加し、`[PROJECT-MEMORY-AUTO-DECISION]` で `quality_guard / route_guard / conversation_intent / refresh_executed` を観測可能にした。`ProjectMemoryAutoUpdater::shouldRefreshFromEvaluation()` には `logger / context` を渡せるようにし、normal / analysis / history summary / advanced finalizer 経由で `project_id / thread_id / route_detail / conversation_intent_profile` を decision log に載せられるようにした。既存の `[PROJECT-MEMORY-AUTO]` と `[PROJECT-MEMORY-AUTO-SKIP]` は維持し、refresh 採否ロジック自体は大きく変更していない。runtime 確認では read-only 系で `conversation_intent / route_guard / quality_guard` のいずれかにより `project_meta` の不要更新が防がれることを確認し、`それは違います。質問の因数分解に戻りたいです。` では `conversation_intent_correction` による decision log を確認した。artifact 系 `csv化してください` では `request_type=artifact`、`todo_policy_hint=allow`、`[PROJECT-MEMORY-AUTO-DECISION] action=refresh | guard=none | reason=refresh_executed` を確認し、`ai_project_agents_auto_md / ai_project_readme_auto_md / ai_project_todo_auto_md` の auto 3 種が更新されることを確認した。command 系 `質問分解ロジックを改善してください。` では `request_type=command`、`todo_policy_hint=allow`、read-only skip では止まっていないこと、`JUDGE-NORMAL-START` までは確認できたが、SSE timeout により final decision log は未取得だった。さらに `50bc77f fix(chat): use conversation intent for route selection` で `chat.php` から `ChatRouteSelector` へ `conversation_intent_profile` を渡し、selector 側に read-only intent consultation route 補正を追加した。対象は `request_type=consultation` かつ `todo_policy_hint=read_only` のうち `new_request / status_check / rollback / correction` で、強い `CSV / report / doc_extract / artifact` signal がある場合は既存 route を維持する。これにより `それは違います。質問の因数分解に戻りたいです。` が `selector_complex_advanced` に誤流入していた問題を `normal_rag.project_memory_consultation` へ補正し、`[ROUTE-SELECTED]` ログで `route / route_detail / request_type / relation / todo_policy_hint / selector_reasons` を観測可能にした。reason code として `selector_intent_read_only_consultation` を追加し、class-level では `この方針は自然ですか？`、`次はどうしましょうか？`、`戻っても良いでしょうか？`、`それは違います。質問の因数分解に戻りたいです。` がいずれも `normal_rag.project_memory_consultation` へ補正されること、`csv化してください` は `data_analysis`、`このCSVを集計して詳細を教えてください。` は `data_analysis`、`この内容を報告書にしてください。` は `advanced_hybrid` の既存 route を維持することを確認した。続く `7c6d2b5 improve(chat): classify code improvement requests` では `ConversationIntentInterpreter` に `implementation_request / code_improvement` 系の narrow rule を追加し、`looksLikeCodeImprovementCommand()` を新設して `looksLikeCommand()` に code improvement 判定を接続した。`detectUserIntent()` では該当ケースを `code_improvement` に寄せ、`detectExpectedResponse()` では `implementation_plan`、`detectTodoPolicyHint()` では `allow` 扱いにした。あわせて `looksLikeConsultation()` に `この方針は改善した方がよい` 系を追加し、相談系の誤分類を防止した。class-level では `質問分解ロジックを改善してください。`、`チャットの回答生成ロジックを改善してください。`、`ProjectMemoryAutoUpdater の overcapture を直してください。`、`ProjectMemoryAutoUpdater を修正してください。`、`route選択ロジックを直してください。` がいずれも `command / code_improvement / implementation_plan / allow / needs_todo=true` に寄ることを確認した。`この方針は自然ですか？`、`この方針は改善した方がよいですか？`、`次はどうしましょうか？`、`戻っても良いでしょうか？`、`それは違います。質問の因数分解に戻りたいです。` は `consultation / read_only` のまま維持し、`csv化してください`、`このCSVを集計して詳細を教えてください。`、`この内容を報告書にしてください。` も既存分類維持を確認した。`7e190db improve(chat): include intent strategy in normal prompts` では `PromptManager::buildResponsePriorityHeader()` に optional intent profile 引数を追加し、`chat_normal.php` から既存の `conversationIntentProfile` を渡すようにした。`request_type / user_intent / expected_response / answer_strategy` から短い intent instruction を生成し、full JSON は prompt に入れず、`unknown` 値も過剰に出さない。`todo_policy_hint` は今回は prompt に直接出しておらず、対象は normal route のみで、`chat_analysis.php / chat_advanced.php / AdvancedRouteFinalizer.php` への展開は未実施。runtime では `ProjectMemoryAutoUpdater の overcapture を直してください。` で `request_type=command`、`user_intent=code_improvement`、`expected_response=implementation_plan` と prompt 内の intent instruction を確認し、final answer は現状課題 / 修正方針 / 実装ステップ / 検証方法 / 残課題に寄った。`質問分解ロジックを改善してください。` でも `command / code_improvement / implementation_plan` の instruction が prompt に入ることを確認したが、project 付き normal route が重く、今回の runtime 枠では final result までは未取得だった。`この方針は自然ですか？` では `request_type=consultation`、`expected_response=proposal`、`route=normal_rag.project_memory_consultation` と、`すぐ実装扱いにせず、方針確認・リスク・次の選択肢を示す` 系 instruction を確認し、回答も妥当性評価 / リスク / 次の一手に寄った。`次はどうしましょうか？` では `relation=status_check` と現在地整理 + 次アクション提案への寄りを確認した。`それは違います。質問の因数分解に戻りたいです。` では correction 系の realign instruction が入り、回答も指摘受容 / 論点リセット / 因数分解へ戻す方向に寄った。`戻っても良いでしょうか？` でも rollback 意図に沿って元の論点へ戻る回答になった。`[OLLAMA-RAW-PROMPT]` では full JSON が入っておらず、追加は短い 3 行程度で、`priorityChars` はおおむね 266〜295 の範囲に収まり prompt 肥大化は見られなかった。artifact / CSV / report 影響として、`csv化してください` は `data_analysis.csv_export_request` を維持し、normal prompt 改善による CSV 系への悪影響は見えなかった。一方で `この内容を報告書にしてください。` は `advanced_hybrid.doc_extract` に流れており、これは今回の normal prompt 改善の副作用ではなく既存 report route / factorizer 側の残課題として整理する。残りは、将来 `TODO` だけ抑止して `agents / readme / memory` は更新できる finer-grained 制御を検討すること、`follow_up` の扱いを実運用ログでさらに調整すること、`answer_strategy` の prompt 反映を analysis / advanced へ広げるか判断すること、`資料メモを開いて...` の前提操作吸収、clarification 文面の自然化、command 系で `JUDGE-NORMAL-START` 後の `quality_guard / refresh_executed` を runtime で 1 件取り切ること、read-only / command / artifact の route 選択改善、actionable 系が `quality_guard` で止まりすぎていないかの継続確認、SSE の curl デバッグが timeout しやすいため runtime debug 方法を改善すること、code_improvement 用 `route_detail` を新設するか検討すること、`normal_rag` に残る実装依頼を route 選択でどう扱うか、code_improvement 判定を使って将来的に `implementation_plan` 返答や専用 route に寄せるか検討すること、次段では軽い command 入力で `quality_guard` 分岐の実ログを補完するか route 分類改善に進むこと。 |
| P1 | 実装済み（要改善） | mixed thread で read-only consultation が `advanced_hybrid.multi_source_advice` に誤流入する問題を抑え、`normal_rag.project_memory_consultation` へ戻す selector override を最小変更で追加する | `AI_System_Data/src/ChatRouteSelector.php` | `7b77bf1 fix(chat): keep read-only consultations on normal route` で `ChatRouteSelector.php` のみを最小変更し、`advanced_hybrid.multi_source_advice` だけ read-only consultation override 対象に戻した。`request_type=consultation` かつ `todo_policy_hint=read_only` のとき、mixed thread で factorizer が `advanced_hybrid.multi_source_advice` を立てても `normal_rag.project_memory_consultation` へ補正する。一方で `advanced_hybrid.doc_extract / advanced_hybrid.history_report` は override せず、PDF doc_extract / CSV集計・export / report / artifact route も維持した。selector reason として `selector_intent_read_only_consultation_multi_source_advice_override` を追加し、selector evidence に `factorized_multi_source_advice`、ログには `factorized_route=advanced_hybrid.multi_source_advice` が残る。class-level では mixed history ありの `この方針は自然ですか？` と `この方針は改善した方がよいですか？` で `factorizer=advanced_hybrid.multi_source_advice` でも `selector=normal_rag.project_memory_consultation` に補正されることを確認した。既存維持として `次はどうしましょうか？`、`戻っても良いでしょうか？`、`それは違います。質問の因数分解に戻りたいです。` は `normal_rag.project_memory_consultation` を維持した。非対象維持として `PDF図面2.pdf の要点を教えてください。` は doc_extract、`csv化してください` は csv export、`この内容を報告書にしてください。` は既存 advanced / artifact 系を維持した。runtime でも `api/chat.php` 経由の mixed thread で `この方針は自然ですか？` と `この方針は改善した方がよいですか？` がいずれも `normal_rag.project_memory_consultation` に入り、相談回答として保存され、`advanced_hybrid.multi_source_advice` への誤流入は再現しなかった。結論として consultation / read-only intent 自体は正しく分類されており、誤流入の原因は factorizer が mixed thread の recent history から `recent_project_asset_context` を拾い、recommendation keyword と合わせて `advanced_hybrid.multi_source_advice` を立てたこと、selector が `advanced_hybrid.*` を一律で read-only override 対象外にしていたことだった。残りは `hasRecentProjectAssetContext()` を狭めるかの設計、clean / mixed thread の追加 runtime 継続確認、read-only でも本当に multi_source_advice が必要な境界整理、`この方針は改善した方がよいですか？` の継続回帰確認、`normal_rag.project_memory_consultation` の回答品質と長さ調整、`thread_id` 省略時に既存 thread へ継続される挙動の整理。 |
| P1 | 実装済み（要改善） | code_improvement / implementation_plan 時の `projectOperatingMemoryPrompt` を短縮し、artifact 成果品レーンのバイアスを弱める short memory policy を normal route に追加する | `AI_System_Data/public/api/chat_normal.php` | `ebbc3ae fix(chat): shorten project memory for code improvement prompts` で `chat_normal.php` のみを最小修正し、`user_intent === 'code_improvement'` かつ `expected_response === 'implementation_plan'` のときだけ `projectOperatingMemoryPrompt` を short mode にするようにした。route / selector / `PromptManager` / `ConversationIntentInterpreter` / DB schema は未変更のまま、CSV / PDF / 報告書などの artifact 成果品レーンを弱め、開発方針・実装相談・TODO系の文脈は残す構成にした。`[PROJECT-MEMORY-CONTEXT-POLICY] policy=code_improvement_short_memory` で観測可能にした。class-level では `ProjectMemoryAutoUpdater の overcapture を直してください。` 相当で `project memory: 6025 chars -> 1540 chars`、artifact bias 除去、implementation context 維持を確認し、`この方針は自然ですか？` の consultation 系では full memory のまま維持されることも確認した。runtime では `api/chat.php` 経由で `ProjectMemoryAutoUpdater の overcapture を直してください。` を確認し、HTTP 200、final result 到達、`sourceCount=0`、回答が実装計画寄りであることを確認し、PDF / CSV / consultation では short mode が発火しないことも確認した。回帰確認として `PDF図面2.pdf の要点を教えてください。` は PDF route、`このCSVを集計して詳細を教えてください。` は CSV 集計 route、`この方針は自然ですか？` は consultation route を維持した。一方で runtime の main target は完走したものの、経過時間は約 `169319ms` で前回より明確に速くなったとは言えず、`projectOperatingMemoryPrompt` の artifact ノイズは抑えられたが、重さの主因はまだ残る。残りは、projectOperatingMemoryPrompt 短縮だけでは runtime 重さは十分に下がっていないため次は judge / rewrite の重さを優先して調査すること、`long_answer` judge policy の code_improvement 例外化、`normal_rag.code_improvement` 専用 policy の切り出し、response length / max token / answer shape の制御、client timeout 120秒と server 側 generation + judge 合計時間の不整合整理、必要なら PDF / CSV / consultation の fresh runtime 回帰確認を追加すること。 |
| P1 | 実装済み（要改善） | `advanced_hybrid` 内で CSV metadata / advisory 質問を SQL 生成前に short-circuit する SQL admission gate を追加する | `AI_System_Data/public/api/chat_advanced.php`, `AI_System_Data/src/AdvancedFastPathResolver.php` | `chat_advanced.php` で `decomposeQuestion()` / SQL生成前に `resolveSqlAdmissionGate()` を呼ぶ導線を追加し、`AdvancedFastPathResolver.php` に gate 用 public wrapper と `csv_metadata_advisory` 判定を追加した。CSVログ構造比較 / CSV分析観点 / データ傾向相談を SQL 生成前に short-circuit し、`T1.ToolSource` のような JSON key 物理カラム誤認 SQL 生成を抑止した。`AdvancedSqlSubQueryRunner` / SQL repair / planner parse failure に進ませず、ログは `[ADV-SQL-GATE] blocked=csv_metadata_advisory ...` を出す。確認済み: `git diff --check` OK、Docker 内 `php -l` OK、synthetic で `Alli、SecureMemo、サテライトAIのログにはどのような情報が記録されていますか` と `このデータの傾向を教えてください` は gate 該当、`出荷一覧表を月別に集計してください`、`この資料の要点を教えてください`、`2025年3月の件数を出してください`、`aaa.csv の い 列を集計してください`、`これまでの会話履歴を要約してください` は gate 非該当を確認した。さらに `8753147 improve(router): route csv log metadata compare requests to advanced` 反映後の fixture live E2E では、`Alli、SecureMemo、サテライトAIのログにはどのような情報が記録されていますか` が `normal_rag` に落ちず `advanced_hybrid.multi_source_advice` に到達し、`factorized_route=advanced_hybrid.multi_source_advice`、`operation=csv_log_metadata_compare`、`selected_route=advanced_hybrid.multi_source_advice`、`[ADV-FASTPATH] csv_log_metadata_compare matched | csv_files=3 | common_keys=8 | diff_keys=9`、`[ADV-SQL-GATE] blocked=csv_metadata_advisory | route=advanced_hybrid | reason=fastpath_csv_log_metadata_compare | fast_path=csv_log_metadata_compare` を確認した。`T1.ToolSource` 系 SQL、`[SQL-EXEC-FAILED]`、`[CRITICAL-LOOP]`、`doc_chunks fallback` は出ず、回答本文も fixture 3CSV に沿って各CSVの主項目 / 共通項目 / 差異項目 / 統合キー候補 / `Timestamp / UserID / ToolSource / LogID / SessionID` の注意点 / 次に確認すべき列を返した。補助確認の `このデータの傾向を教えてください` でも `factorized_route=advanced_hybrid.multi_source_advice`、`operation=analysis_recommendation`、`[ADV-FASTPATH] csv_metadata_advisory matched`、`[ADV-SQL-GATE] blocked=csv_metadata_advisory | reason=fastpath_csv_metadata_advisory` を確認し、SQL生成なし・`doc_chunks fallback` なしだった。fixture project は削除済みで、`projects / chat_threads / chat_history / project_csv_files / project_csv_rows / documents / doc_chunks` は残件 0、`git status --short` も空を確認済み。関連commitは `b6c8bf0 fix(advanced): gate csv metadata and advisory requests before sql`。残りは fixture E2E確認済みだが実運用データ確認待ちであること、必要に応じて `AdvancedFastPathResolver` の advisory / metadata 語彙を実ログで微調整すること、実運用CSVで無関係CSVへ引っ張られないか確認すること。 |
| P2 | 実装済み（要改善） | `support.php` の `資料` タブ / `PDF` タブ preview 非表示問題を修正し、動的追加時の preview 再描画を安定化する | `AI_System_Data/public/assets/js/modules/chat.js`, `AI_System_Data/public/assets/js/support.js`, `AI_System_Data/public/assets/js/modules/ui.js`, `AI_System_Data/public/support.php` | `8e73222 fix(support): restore material and pdf previews` で `chat.js` のみを最小修正し、資料メモ dynamic card の属性名 `data-material-doc-id` と `data-material-document-id` の不一致を解消した。`activate: true` 時に `loadMaterialDocument(documentId)` を呼んで資料 preview を再読込するようにし、PDF dynamic card の iframe 経路も既存の `/api/view_pdf.php?...` に統一した。あわせて PDF dynamic card の `<details>` を `open` にして、preview が畳まれたまま見えにくい状態を避けた。確認では `support.php` の DOM、`support.js` の selector、`chat.js` の動的追加 DOM、`ui.js` の `openSourceDocumentTab` / `openPdfTab` 経路、`window.loadMaterialDocument` のグローバル公開、`support.php` の `php -l`、`support.js / ui.js / chat.js` の構文パースに加えて、実ブラウザで資料タブ preview / PDF タブ preview の表示とタブ切り替え後の復帰、チャット / CSV / FAQ / project memory 周辺に明らかな回帰がないことを確認した。backend や DB schema には未変更。残りは実運用ブラウザでの継続確認、手動アップロード PDF とチャット生成 PDF の混在確認、他タブ回帰確認。 |
| P2 | 実装済み（要改善） | Docker 上の Playwright Chromium で `support.php` の中央 UI を自動検証できる E2E 基盤を追加する | `.gitignore`, `docker-compose.yml`, `package.json`, `package-lock.json`, `playwright.config.js`, `tests/e2e/support-tabs.spec.js` | `8a6d502 chore(e2e): add docker playwright support tests` で、Codex 環境では in-app browser / extension browser / computer-use が安定しない前提を補うため、Docker 上の Playwright Chromium で実ブラウザに近い E2E 検証を回せるようにした。`e2e` サービスから `http://app/support.php` にアクセスし、概要 / ナレッジ / 運用メモ / 資料 / PDF / CSV タブ切替、ナレッジ `details` 展開、運用メモの主要フィールド（`案件内容` / `AIエージェント` / `タスク一覧`）、PDF の `iframe` または欠落メッセージ、console error 監視、404 監視、失敗時の screenshot / trace 保存を行う最小 E2E を追加した。確認済み: `docker compose run --rm e2e npm install` 成功、`docker compose run --rm e2e npx playwright test` 成功、最終結果は `4 passed (14.4s)`。E2E 側では、`@playwright/test` の semver が広すぎて Playwright image と不一致になったため依存関係を調整し、`login.php` が `label for` 非対応で `getByLabel()` が効かなかったため実 DOM に合わせてログイン操作を調整し、FAQ アコーディオンも「必ず前カードが閉じる」と決め打ちしないようにした。失敗時の成果物は `test-results/` 配下に残り、例として `test-results/support-tabs-support-php-t-f0ff2-ame-or-missing-file-message/test-failed-1.png` を確認済み。ただし `test-results/` は通常 commit 対象にしない運用とする。残りは `SUPPORT_E2E_PASSWORD` が既定で `admin123` のローカル開発前提であること、本番系や別 seed では環境変数上書き運用へ寄せること、今回は support 中央 UI の確認に限定していること、チャット送信起点の動的更新は別 spec として追加すること、PDF 実ファイル欠落の上流原因調査は別 P1 タスクで扱うこと、本番 Windows Chrome での最終目視確認は引き続き必要であること。 |
| P1 | 実装済み（要改善） | `documents` と物理 PDF ファイルの整合性を追跡するため、報告書生成ログ強化と読み取り専用 integrity check を追加する | `AI_System_Data/src/ReportGenerator.php`, `AI_System_Data/src/DocumentFileIntegrityChecker.php`, `AI_System_Data/src/ReportRegenerationCandidateFinder.php`, `AI_System_Data/scripts/check_document_files.php`, `AI_System_Data/public/api/document_integrity_check.php`, `AI_System_Data/public/api/report_regeneration_candidates.php`, `AI_System_Data/public/support.php`, `AI_System_Data/public/assets/js/support.js` | `090a135 improve(documents): add report save logging and file integrity check` で、recent PDF に `documents` レコードと `doc_chunks` はあるが実PDFと同名HTML fallbackが無い問題に対し、既存データ修復や DB schema 変更をせず、追跡と検知だけを追加した。背景として `documents.id=23/24/25` は DBレコードあり・`doc_chunks` あり・実PDFなし・同名HTML fallbackなし・`view_pdf.php` では `file path resolution failed`、一方 `documents.id=6` は実ファイルありで preview 可能だった。`ReportGenerator.php` では `AppLogger` を使い、`[REPORT-GENERATOR] html_saved / pdf_saved / pdf_missing_after_generation / document_registered / chunks_registered` を `chat_debug.log` に記録するようにし、生成時点で HTML/PDF が存在したか、PDFサイズ、登録された `document_id`、`doc_chunks` 登録件数を後から追跡できるようにした。あわせて `AI_System_Data/scripts/check_document_files.php` を追加し、`documents.file_path LIKE '%.pdf'` を読み取り専用で走査して PDF 実体の有無と同名HTML fallback の有無を確認し、標準出力へ `[OK] / [MISSING] / Summary`、ログへ `[DOC-INTEGRITY]` を出すようにした。DB更新・実ファイル削除・自動復旧は行わず、新しい runtime ログファイルも増やさず既存 `chat_debug.log` に寄せている。確認済み: Docker内 `php -l` で `AI_System_Data/src/ReportGenerator.php` と `AI_System_Data/scripts/check_document_files.php` は OK、`git diff --check` OK、CLI 実行では `id=6` が `OK`、`id=23/24/25` が `MISSING`、`Summary: ok=1 missing=3 html_fallback=0` を確認した。確認で追記された `chat_debug.log` は restore して commit 対象外にし、最終変更ファイルは上記 2 件のみに限定した。さらに `774f64f improve(documents): expose file integrity check for operations` で、運用者が `support.php` から missing PDF を読み取り専用で見つけられる導線を追加した。`AI_System_Data/src/DocumentFileIntegrityChecker.php` を追加し、PDF documents の走査・`file_path` 解決・PDF 実体有無確認・同名HTML fallback確認・summary / missing list 生成を共通化した。`AI_System_Data/scripts/check_document_files.php` は新 checker を再利用する形へ変更し、`[OK] / [MISSING] / Summary` と `[DOC-INTEGRITY]` ログ出力を維持した。`AI_System_Data/public/api/document_integrity_check.php` では管理者向け read-only API を追加し、`GET` のみで `total / ok / missing / html_fallback` と missing records 一覧（`document_id / project_id / title / file_path / html_fallback / created_at`）を返すようにした。`support.php` の PDF タブには admin 専用の `PDFファイル整合性チェック` パネルを追加し、`support.js` から必要時だけ API を呼んで summary と missing records 一覧を表示するようにしたため、毎回ページ表示時に全件走査しない。確認済み: `git diff --check` OK、Docker内 `php -l` で `AI_System_Data/src/DocumentFileIntegrityChecker.php`、`AI_System_Data/scripts/check_document_files.php`、`AI_System_Data/public/api/document_integrity_check.php`、`AI_System_Data/public/support.php` は OK。CLI 確認では `id=6` は `OK`、`id=23/24/25` は `MISSING`、`Summary: ok=1 missing=3 html_fallback=0` を確認した。API 確認では admin ログイン後に Docker コンテナ内から `document_integrity_check.php` を確認し、`summary.total=4 / ok=1 / missing=3 / html_fallback=0` を確認した。UI 静的確認では `support.php?project_id=4&tab=pdf` の HTML に `document-integrity-panel`、`document-integrity-refresh`、`PDFファイル整合性チェック` が出力されることを確認した。`644aa06 improve(support): show missing report pdf regeneration guidance` では、`documents.id=23/24/25` が通常アップロードPDFではなく ReportGenerator 経由の AI報告書レコードであり、`2026-06-18 21:02:29`、`2026-06-18 21:21:54`、`2026-06-19 07:05:24` の「会話履歴を要約して報告書化するための構成案を作る」系の依頼直後に作られていたことを踏まえ、`support.php` の PDF カード一覧を改善した。`documents` レコードあり・`doc_chunks` あり・`file_path=report_*.pdf`・実PDFなし・同名HTML fallbackなし、かつ `ReportGenerator.php` は PDF 実体確認後に `documents` へ insert するため、最初から壊れて登録されたというより生成時には存在し後から失われた可能性が高い。一方で `delete_pdf.php` で削除すると `documents` と関連 `doc_chunks` まで消えるため、現時点では削除よりも再生成誘導が安全と判断した。実装では PDFカード一覧に `PDF / HTML代替 / 欠落 / 再生成推奨` の状態バッジを追加し、missing の PDF カードは初期展開するようにした。AI報告書由来の missing には「削除より先に再生成を優先する」旨を明示し、`documents.id=23/24/25` を通常PDFと見間違えず、欠落している成果物を消さずに記録を残したまま、運用者が再生成を先に検討できるようにした。静的確認では `git diff --check -- AI_System_Data/public/support.php` OK、Docker内 `php -l /var/www/html/public/support.php` OK、変更ファイルは `AI_System_Data/public/support.php` のみ。Docker Playwright では `support.php?project_id=4&tab=pdf` を開き、`id=6` は通常PDFとして表示され `iframe` 1件・missing 文言なし、`id=23/24/25` は 3件とも missing 状態で表示され、missing カードは `open=true` で初期展開、`再生成推奨` と `削除より先に再生成を優先してください` が 3件とも表示されること、Console error なし、`PDFファイル整合性チェック` パネルは引き続き表示され `チェックを実行` ボタン動作 OK、`document_integrity_check.php` は 200、`integrityResponses` は 200 を 2回確認、`TOTAL / OK / MISSING / HTML FALLBACK` 表示あり、missing records に `23/24/25` を確認、表示崩れなし、新しいファイル変更なしを確認した。加えて `6fe7b14 improve(support): show report regeneration candidates` では、missing AI報告書に対する再生成候補判定を read-only で可視化した。背景として `documents.id=23/24/25` は通常アップロードPDFではなく ReportGenerator 経由の missing AI報告書で、`id=23 -> user chat_history.id=404 / assistant chat_history.id=405`、`id=24 -> user 408 / assistant 409`、`id=25 -> user 424 / assistant 425`、いずれも `thread_id=2` に属し、会話履歴要約・報告書構成案系の依頼直後に作成されていた。assistant 側には `auto-reason_*` の reasoning session と `chat_reasoning_steps` が残っている一方、元の `report_*.html` は失われているため、`exact replay` ではなく `near-rebuild / semantic rebuild` が現実的と判断した。方針は「既存 missing record は残す」「既存 `doc_chunks` も残す」「再生成する場合は新規 `documents` として作る」「既存 `documents` を UPDATE しない」「まずは read-only の再生成候補判定を表示する」。そのため `AI_System_Data/src/ReportRegenerationCandidateFinder.php` を追加し、`documents.created_at` 近傍の `chat_history` を照合して user / assistant 候補ペアを推定し、`chat_reasoning_steps` と reasoning session を確認したうえで `high|medium|low|none` の confidence、`mode=near_rebuild`、`exact_replay=false` を返すようにした。`AI_System_Data/public/api/report_regeneration_candidates.php` も追加し、admin 向け・`GET` のみ・read-only で `document_id` 指定から再生成候補情報を JSON 返却するようにした。`support.php` の missing AI報告書カードには `再生成候補の読み取り確認` パネルを追加し、`support.js` から on-demand fetch して `near-rebuild可能`、`exact replay不可`、`user_chat_history_id`、`assistant_chat_history_id`、`reasoning_session_id`、`reasoning_steps_count`、および「再生成する場合は新規 `documents` として扱う」注意文を表示するようにした。確認済み: `git diff --check` OK、Docker内 `php -l` で `AI_System_Data/src/ReportRegenerationCandidateFinder.php`、`AI_System_Data/public/api/report_regeneration_candidates.php`、`AI_System_Data/public/support.php` は OK。class-level 確認では `document_id=23/24/25` はすべて `confidence=high`、`mode=near_rebuild`、`exact_replay=false` となり、対応する `user_chat_history_id`、`assistant_chat_history_id`、`reasoning_session_id`、`reasoning_steps_count` を取得できた。Docker Playwright では `id=6` は通常PDFとして表示され `iframe` 1件、`id=23/24/25` は missing のまま初期展開され、3件とも再生成候補 UI、`near-rebuild可能`、`exact replay不可` が表示されることを確認した。`document_integrity_check.php` は 200 を2回、`report_regeneration_candidates.php?document_id=23/24/25` もすべて 200 を確認し、Console error なし、404 なし、integrity パネル既存動作も維持した。未確認なのは、現 project では `html_fallback=0` のため HTML fallback 対象レコードの実表示を直接確認できていない点。残りは recent report PDF が「生成時は存在して後で消えた」のか「環境持ち越しで欠落した」のかを実ログで継続確認すること、`check_document_files.php` / 新API を定期実行へどう組み込むか整理すること、support 上の診断パネルを必要なら自動更新や件数強調へ寄せること、durable 保存領域の運用整理、既存欠落レコード `documents.id=23/24/25` の削除 / 非表示 / 再生成方針、orphan / missing 検知の通知化、実ブラウザで新パネルのクリック確認と表示崩れ確認、HTML fallback 対象データがある状態での表示確認、再生成導線そのものをどう設計するか、実際の再生成APIを作るかどうか、再生成時に旧 missing record を残す運用をどう明示するか、新規 `documents` として作成するフロー、旧 `doc_chunks` を監査用として残す整理、missing report PDF の non-admin 表示を弱くするか、管理画面側への本格統合を検討すること。 |
| P1 | 実装済み（要改善） | missing AI報告書を既存 record を壊さず near-rebuild で新規 `documents` として再生成し、生成された runtime report artifacts は Git 管理対象から外す | `AI_System_Data/public/api/report_regenerate_missing.php`, `.gitignore` | `1619705 improve(reports): add missing report regeneration endpoint` で、missing AI報告書を admin が明示的に POST した場合だけ near-rebuild として新規 `documents` を作る再生成 API `AI_System_Data/public/api/report_regenerate_missing.php` を追加した。API は admin only / POST only / `X-CSRF-Token` 必須 / 入力は `document_id` で、`ReportRegenerationCandidateFinder` による候補判定が `confidence=high`、`mode=near_rebuild`、`exact_replay=false` の場合のみ user / assistant / reasoning を DB から復元して `ReportGenerator::createFromChat()` を実行し、既存 `documents` は更新せず、既存 `documents` と既存 `doc_chunks` を削除せず、成功時のみ新規 `documents / doc_chunks` を作る。検証では `document_id=25` を 1 回だけ実行し、`old_document_id=25`、`new_document_id=29`、`mode=near_rebuild`、`exact_replay=false` を確認した。旧 `documents.id=25` は残り、旧 `doc_chunks.doc_id=25` は 19 件のまま、新規 `documents.id=29` と新規 `doc_chunks.doc_id=29` 19 件が作成された。新規 PDF 実体は `resolved_path=/var/www/html/public/01_RAG_Documents/4/report_6a3e0a0587d474.49808284.pdf`、`size=85368` を確認し、`check_document_files.php` でも `id=6: OK`、`id=23/24/25: MISSING`、`id=29: OK`、`Summary: ok=2 missing=3 html_fallback=0` を確認した。GET 拒否は `error=post_required`、invalid document は `document_id=0 -> document_id_required`、`document_id=999999 -> document_not_found` を確認済みで、DB schema 変更なし、Docker 内 `php -l` で `AI_System_Data/public/api/report_regenerate_missing.php` は OK、`chat_debug.log` の検証差分は restore 済み。UI 変更はないため Playwright は未実施で、API / DB / file integrity 確認に限定した。加えて `1ae940d chore(gitignore): ignore generated report artifacts` で、再生成 API 検証により生成された runtime report artifacts の Git 管理方針を整理した。`documents.id=29` が参照する `AI_System_Data/public/01_RAG_Documents/4/report_6a3e0a0587d474.49808284.html` と `.pdf` は実ファイルなので削除せずローカルに残しつつ、Git 管理対象にはしない方針とし、`.gitignore` に `AI_System_Data/public/01_RAG_Documents/*/report_*.html` と `AI_System_Data/public/01_RAG_Documents/*/report_*.pdf` を追加した。手動アップロード PDF や `doc_*.pdf` には触れず、runtime 成果物だけを対象にしている。確認済み: `git status --short` で未追跡 `report_*.html / report_*.pdf` が消え、差分は `.gitignore` のみ、`git diff --check -- .gitignore` OK、PHP / JS の変更なし。残りは再生成由来を title や UI でより明示するか、二重再生成 warning の強化、old/new の厳密リンクを schema なしでどこまで見せるか、support UI に再生成ボタンを置くか、再生成 API の Playwright または curl ベース追加テスト、runtime report artifacts の本番・開発での保存運用、将来的な `documents.status / documents.source_document_id` の検討。 |
| P1 | 実装済み（要改善） | `PLANNER-PARSE-FAILED` 時に CSV metadata / advisory 系が `doc_chunks` fallback に脱線しない安全弁を追加する | `AI_System_Data/src/AdvancedRoutePlanner.php`, `AI_System_Data/public/api/chat_advanced.php` | `AdvancedRoutePlanner.php` で planner parse failure 時の fallback 生成を整理し、CSV metadata / advisory family の場合は `doc_chunks` ではなく `project_csv_files` ベースの marker plan を返すようにした。marker は `fallback_family=csv_metadata_advisory` / `fallback_route_hint=csv_metadata_advisory`。`chat_advanced.php` では marker を検知したら `AdvancedFastPathResolver::resolveSqlAdmissionGate()` に再委譲し、resolver が空でも `doc_chunks` に戻さず確認質問へ short-circuit する。PDF / 履歴 / 明示CSV集計の fallback は維持し、ログは `[PLANNER-FALLBACK] family=csv_metadata_advisory | action=marker_plan`、`[PLANNER-FALLBACK] ... | action=fastpath_redelegate`、`[ADV-SQL-GATE] blocked=planner_csv_metadata_fallback ...` を出す。確認済み: `git diff --check` OK、Docker 内 `php -l` OK、reflection で `各AIツールのログ構造を比較してください` と `このCSVをどう分析すればよいですか` は `project_csv_files + fallback_family=csv_metadata_advisory`、`この資料の要点を教えてください` は `doc_chunks` 維持、`これまでの会話履歴を要約してください` は `chat_history` 維持、`出荷一覧表を月別に集計してください` は `project_csv_rows` 維持を確認した。加えて fixture live E2E では、`Alli、SecureMemo、サテライトAIのログにはどのような情報が記録されていますか` が `advanced_hybrid.multi_source_advice` に到達し、`[ADV-FASTPATH] csv_log_metadata_compare matched` と `[ADV-SQL-GATE] blocked=csv_metadata_advisory` が出て、`T1.ToolSource` 系 SQL / `[SQL-EXEC-FAILED]` / `[CRITICAL-LOOP]` / `doc_chunks fallback` が出ないことを確認した。`このデータの傾向を教えてください` でも `[ADV-FASTPATH] csv_metadata_advisory matched`、`[ADV-SQL-GATE] blocked=csv_metadata_advisory`、SQL生成なし、`doc_chunks fallback` なしを確認した。fixture project は削除済みで、`projects / chat_threads / chat_history / project_csv_files / project_csv_rows / documents / doc_chunks` は残件 0、`git status --short` も空を確認済み。関連commitは `fbe3db9 fix(advanced): avoid doc_chunks fallback for csv metadata planner failures`。残りは fixture E2E確認済みだが実運用データ確認待ちであること、実ログで `PLANNER-PARSE-FAILED -> csv_metadata_advisory marker -> fastpath_redelegate` の流れを実運用データでも確認すること、resolver が空になった場合の clarification 文面を運用で微調整すること、実運用CSVで無関係CSVへ引っ張られないか確認すること。 |
| P1 | 実装済み（要改善） | `2025年3月の件数` を `date_histogram + explicit_year_month` のまま自然な件数回答へ整形する改善 | `AI_System_Data/src/CsvAggregationPlanner.php`, `AI_System_Data/src/CsvAggregationAnswerFormatter.php` | `CsvAggregationPlanner.php` に `count_only_request` を追加し、`2025年3月の件数` / `2025-03 の件数` のような件数だけ欲しい質問を `count_only_request=true` にした。一方で `月別 / 日別 / 日別件数 / 推移 / グラフ` 系は `count_only_request=false` にして histogram 表現を維持した。`CsvAggregationAnswerFormatter.php` では `aggregation_mode=date_histogram` かつ `date_filter_mode=explicit_year_month` かつ `count_only_request=true` かつ 1 bucket の場合だけ、短い件数回答へ切り替えるようにした。`filtered_date_count` 専用 mode は追加しておらず、runner / resolver / SQL / `chat_analysis.php` / DB は未変更。関連commitは `9b7207b improve(csv): format explicit period count answers` で push 済み。確認済み: `git diff --check` OK、`php -l` OK、class-level / formatter-level 確認済み、`2025年3月の件数を出してください` は `aggregation_mode=date_histogram`、`date_filter_mode=explicit_year_month`、`date_filter_value=2025-03`、`count_only_request=true` となり、回答本文は `2025-03 の該当件数は 27 件です。` 形式になる。`集計月数 / 最大件数 / 最小件数` の histogram 用説明は出ない。`2025年3月を月別に集計してください` は `count_only_request=false` で histogram 表現維持、`3月分の件数を出してください` は clarification 維持、`登録日と更新日があるCSV` は clarification 維持、`日付列がないCSV` は `missing_date_axis` で clarification 維持。確認後の `git status --short` は空。残りは実チャット保存経路での end-to-end 確認が未実施なこと、`count_only_request` は clarification ケースでも `true` になり得るが出荷は clarification が優先されるため現状問題ないこと、文面を `2025-03` ではなく `2025年3月` 表記に寄せるかは微調整余地があること、将来的に件数系が複雑化したら `filtered_date_count` 専用 mode を再検討すること |
| P1 | 実装済み（要改善） | CSV temporal 第3段として、`2025年3月分` / `2025年3月の件数` の generic な明示年月質問を planner 側で安全に `date_histogram + explicit_year_month filter` へ寄せる | `AI_System_Data/src/CsvAggregationPlanner.php` | `CsvAggregationPlanner.php` の `detectDateFilterMeta()` で `YYYY-MM分 / YYYY/MM分 / YYYY年M月分 / YYYY-MM / YYYY/MM / YYYY年M月 / M月分` の順に近い分類へ見直し、`explicit_year_month` を `month_only` より優先するようにした。年付き月指定では、弱い implicit column match で非日付列へ吸われた場合に date-like 列推定へ安全に戻し、対象CSVと date-like 列が一意に決まるときだけ `aggregation_mode=date_histogram` / `date_filter_mode=explicit_year_month` / `date_filter_value=2025-03` へ寄せるようにした。`2025年3月分を集計してください`、`2025年3月の件数を出してください`、`2025-03 の件数を出してください`、`出荷一覧表の2025年3月分を集計してください`、`出荷一覧表で2025年3月の件数を出してください` は class-level 確認で `target_column=日付`、`date_granularity=month`、`needs_clarification=false` を確認済み。`3月分` / `先月` / `今月` / `今年` は引き続き clarification、複数日付列 / 日付列なし / 複数CSV曖昧も clarification 維持。`出荷一覧表を月別に集計してください`、`出荷一覧表を日別に集計してください`、`出荷一覧表の「年月」列を集計してください`、`aaa.csv の い 列を集計してください` は既存挙動維持。関連commitは `41afbe6 improve(csv-planner): route explicit year-month requests safely` で push 済み。追加の class-level 接続確認では、planner が `date_filter_mode=explicit_year_month / date_filter_value=2025-03` と `aggregation_mode=date_histogram` を返し、runner 相当の filter あり/なし比較で `2025-03` のみ残ることを確認した。残りは `2025年3月の件数` を filtered count 専用 mode にするかの仕様整理、実チャット保存経路での end-to-end 確認、複数日付列や複数CSV曖昧時の文面調整を行う |
| P1 | 実装済み（要改善） | CSV temporal 第2段として、`explicit_year_month` の明示年月 filter を runner 側で実消費できるようにする | `AI_System_Data/src/CsvDateColumnDetector.php`, `AI_System_Data/src/CsvAggregationTargetResolver.php` | `CsvDateColumnDetector.php` に month-only 値の正規化対応を追加し、`2025-03 / 2025/03 / 2025年3月 / 2025年03月` を扱えるようにした。`normalizeDateBucket()` に `!Y-m / !Y-n` 系を追加し、month-only 値が当日依存にならないようにした一方で、既存の `2025-03-01 / 2025/03/01 / 2025年3月1日` の正規化は維持した。`CsvAggregationTargetResolver.php` では `date_filter_mode=explicit_year_month / date_filter_value=YYYY-MM` のときだけ PHP 側 post-fetch filter を適用するようにし、SQL は変更せず、filter なしの既存 `date_histogram` 経路は維持した。関連commitは `ed8296b improve(csv-date): support explicit year-month filtering` で push 済み。確認済み: `git diff --check` OK、`php -l` OK、class-level 確認済み、filter なし `date_histogram` では `2025-03` と `2025-04` の両方が残り、filter あり `date_histogram` では `date_filter_value=2025-03` により `2025-03` のみ残る。`2025-03-01 / 2025/03/01 / 2025年3月1日 / 2025-03 / 2025/03 / 2025年3月` は `2025-03` に属し、`2025-04-01 / 2025/04 / 2025年4月` は `2025-03` filter から除外されること、filter なしの既存挙動は維持されること、コード変更なしの確認後に `git status --short` が空であることを確認した。確認結果例: filter なし `{ \"2025-03\": 27, \"2025-04\": 27 }`、filter あり `{ \"2025-03\": 27 }`。残りは `2025年3月の件数` を filtered count 専用 mode として扱うか仕様決めが必要なこと、実チャット保存経路で `2025年3月` filter が効くかは未確認なこと、複数日付列がある場合は引き続き clarification 維持であること、`3月分 / 先月 / 今月 / 今年` は引き続き clarification 維持であること、`曜日別 / 午前午後 / 時間帯別` の実集計対応は別タスクとして残る |
| P1 | 実装済み（要改善） | CSV の曖昧質問を、誤った概況回答ではなく確認質問へ倒しつつ、CSV相談系を advisory / `multi_source_advice` 側へ寄せる第1〜第2段 | `AI_System_Data/src/CsvAggregationPlanner.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/src/ChatRouteFactorizer.php` | `CsvAggregationPlanner.php` に `needs_clarification / clarification_reason / missing_dimensions` を追加し、`chat_analysis.php` で `csv_agg` 実行直前に runner へ進まず確認質問を返すようにした。`件数をまとめてください` / `カテゴリ別に集計してください` / `売上を見てください` など、条件不足の集計要求を clarification 対象にした一方で、`出荷一覧表を月別に集計してください` / `出荷一覧表の「年月」列を集計してください` / `aaa.csv の い 列を集計してください` など、明示条件がある集計は従来どおり通ることを確認した。暗黙の1文字列名補完禁止も維持した。さらに `ChatRouteFactorizer.php` に CSV相談系 advisory 語彙を追加し、`このCSVを分析してください` / `このデータの傾向を教えてください` / `この表の問題点を教えてください` / `どの列を見ればいいですか` / `出荷一覧表を分析してください` を `advanced_hybrid.multi_source_advice` へ寄せるようにした。`CSVの内容をいい感じにまとめてください` は `data_analysis.csv_summary` を維持し、`csv_agg / csv_summary` の既存挙動を壊さない範囲で実装した。確認済み: `git diff --check` OK、`php -l` OK、synthetic / class-level / resolver付き確認済み、変更は対象ファイルに限定済み。関連commitは `448719d fix(csv): clarify ambiguous aggregation requests`、`a42fb22 improve(router): route csv advisory questions`。残りは実チャットで `[CSV-CLARIFICATION]` ログを1回確認すること、実チャットで CSV advisory route が `advanced_hybrid.multi_source_advice` に入ることを1回確認すること、`さっきのCSVを分析して` のような recent CSV context 前提の follow-up を確認すること、必要なら `ChatRouteSelector.php` の route lock 回避条件を advisory 用に微調整すること、確認質問文面の改善、CSV曖昧質問が `project_meta / FAQ` に不要反映されないかの継続確認を行う |
| P2 | 実装済み（要改善） | evaluator fallback が未完了回答を通してしまう条件の補強 第1段として、`chat_normal.php` / `chat_analysis.php` / `AdvancedRouteFinalizer.php` に downstream fallback guard を追加する | `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/src/AdvancedRouteFinalizer.php` | `chat_normal.php / chat_analysis.php / AdvancedRouteFinalizer.php` に downstream fallback guard を追加した。`evalResult` が `null / 空` の場合を `missing_eval` として扱い、`evaluation_mode=fallback` を `evaluation_mode_fallback`、`evaluation_source=judge_fallback` を `judge_fallback` として検出するようにした。judge fallback / missing eval の場合、`report / pdf / csv` などの成果品生成と `project_meta refresh` に進まないようにし、`[EVAL-FALLBACK-GUARD] blocked=...` ログを追加した。`ChatEvaluator.php` の fallback shape と、最終回答本文の返却フロー自体は今回は変更していない。確認済み: `missing_eval -> missing_eval`、`evaluation_mode=fallback -> evaluation_mode_fallback`、`evaluation_source=judge_fallback -> judge_fallback`、`evaluation_mode=real, verdict=pass -> null`。`NormalStreamingRouteProcessor / AdvancedReasoningRouteProcessor / AdvancedRouteFinalizer` で判定が一致し、`AdvancedRouteFinalizer` では `blocked=report_generation / blocked=csv_generation` の実ログを確認した。`real/pass` 正常ケースでは guard reason が `null` のままであることも確認済み。検証はコード変更なし・DB書き込みなしの synthetic / class-level 確認で実施した。残りは fallback / missing eval 時に最終回答本文そのものを確認質問へ倒すか、`ChatEvaluator.php` の fallback shape を見直すか、incomplete answer 判定を `LightweightFinalAnswerGuard` 側へ共通化するかの検討と、`project_memory_refresh` の blocked 実ログの end-to-end 確認を行う |
| P1 | 実装済み（要改善） | fallback / missing eval 時に、未完了回答や操作案内だけの回答を最終回答として通さず確認質問へ倒す第1段として、`chat_normal.php` / `chat_analysis.php` / `AdvancedRouteFinalizer.php` に final-answer guard を追加する | `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/src/AdvancedRouteFinalizer.php` | `chat_normal.php / chat_analysis.php / AdvancedRouteFinalizer.php` に fallback / missing eval final-answer guard を追加した。`getDownstreamFallbackGuardReason()` を起点に、fallback / missing eval のときだけ最終回答本文を追加検査するようにし、unsafe な本文だけを短い確認質問へ差し替えるようにした。差し替え時は `[EVAL-FALLBACK-GUARD] blocked=final_answer | route=... | reason=... | unsafe=...` を出す。`needs_revision=true` の場合は既存 `EvaluationRecoveryCoordinator` / recovery flow を優先し、この guard では差し替えない。`real/pass` の正常回答は差し替えないことを確認済みで、既存の `report / pdf / csv / project_meta refresh` の fallback guard は維持した。`ChatEvaluator.php` / `LightweightFinalAnswerGuard.php` / `ClarificationQuestionBuilder.php` は今回は変更していない。確認済み: `git diff --check` OK、`php -l` OK、synthetic / class-level 確認済み、`evaluation_mode=fallback` + 実内容なし本文は確認質問へ差し替わる、`evalResult=null` + 操作案内のみ本文は確認質問へ差し替わる、`evaluation_source=judge_fallback` + 未閉じ code fence は確認質問へ差し替わる、`needs_revision=true` では差し替えなし、`real/pass` では差し替えなし。関連commitは `8eeef66 fix(evaluator): guard fallback final answers`。残りは実チャットで `[EVAL-FALLBACK-GUARD] blocked=final_answer` の実ログを1回確認すること、確認質問文面を route / intent 別に自然化すること、`LightweightFinalAnswerGuard.php / ClarificationQuestionBuilder.php` への共通化を検討すること、手順説明だけ / 途中切れ / 未閉じ構文の判定を実例ベースで微調整すること、`ChatEvaluator.php` の fallback shape を今後見直すか検討すること。あわせて未確認の実ログ確認リストとして `[CSV-CLARIFICATION]` 実ログ、CSV advisory route の実チャット確認、`[EVAL-FALLBACK-GUARD] blocked=final_answer` 実ログを残す |
| P1 | 実装済み（要改善） | `project_meta` ベースの自動更新安定化の第1〜第2段として、`history_summary` 後の `project memory` refresh を判定付きにし、`ProjectMemoryAutoUpdater.php` に before/after の軽い diff ログを追加する | `AI_System_Data/src/ProjectMemoryAutoUpdater.php`, `AI_System_Data/public/api/chat_history_summary.php` | `chat_history_summary.php` の無条件 `ProjectMemoryAutoUpdater::refresh()` をやめ、`shouldRefreshFromEvaluation()` を通すようにした。履歴要約後の `project_meta` refresh は判定付きになり、`ProjectMemoryAutoUpdater.php` には before/after の軽い diff ログを追加した。`[PROJECT-MEMORY-AUTO] refreshed` ログに加えて、`[PROJECT-MEMORY-AUTO] diff` ログで `changed / lane / target / task / next / chars` を確認できるようにした。確認済み: `これまでの会話履歴を要約してください。` で `history_summary` 後の refresh が `shouldRefreshFromEvaluation()` を通って動作すること、`この案件の現在の進行中タスクは何ですか？次に何をすべきですか？` で `normal_rag.project_memory_consultation`、`FINAL-GUARD pass`、`project_meta refresh`、diff ログ出力を確認した。`project_meta.updated_at` の更新も確認済みで、`chat_debug.log` や一時ファイルは残していない。残りは diff ログの粒度と長さの実運用調整、主成果品 / 主対象 / 進行中タスク / 次アクションの抽出安定化、`advisory / material / csv / pdf` レーン切り替わりの継続確認、必要に応じた `buildTodoState()` / `resolveActiveArtifactLane()` の微調整を行う |
| P1 | 実装済み（要改善） | `prompt packet` 優先順位制御の第1〜第5段として、`PromptManager.php` に `buildResponsePriorityHeader(...)` を追加し、`chat_normal.php` の user prompt 先頭へ `【回答優先ガイド】` と軽量 intent instruction を試験導入しつつ、code_improvement 系では normal route の documents/doc_chunks vector RAG、同一 thread の PDF/doc_extract 系 assistant 履歴ノイズ、normal route / evaluator の phase timing を観測・抑制する | `AI_System_Data/src/PromptManager.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/src/ChatEvaluator.php` | `【回答優先ガイド】` に `質問` / `route/detail` / `operation` / `主成果品` / `進行中タスク` / `主根拠` が入ることを実装済み。`project_memory_consultation` と `material_workflow` では `[OLLAMA-RAW-PROMPT]` 先頭にガイドが入り、既存の `history / question` が残ること、`[PROMPT-BUDGET]` に `priority` セグメントが出ること、`FINAL-GUARD` が pass することを確認済み。さらに `5aa0311 fix(chat): enforce project scope in reasoning context` で current project scope instruction を追加し、通常回答 / analysis / advanced の prompt に「現在の `project_id` 以外の `CSV / PDF / documents / project_meta / chat_history / reasoning steps` を根拠にしない」方針を明示した。`[PROJECT-SCOPE]` / `[PROJECT-SCOPE-WARN]` により warning + safe fallback を観測できる第1段まで導入済み。`7e190db improve(chat): include intent strategy in normal prompts` では `PromptManager::buildResponsePriorityHeader()` に optional intent profile 引数を追加し、`chat_normal.php` から既存の `conversationIntentProfile` を渡すようにした。`request_type / user_intent / expected_response / answer_strategy` から短い intent instruction を生成し、full JSON は prompt に入れず、`unknown` 値も過剰に出さない。`todo_policy_hint` は今回は prompt に直接出しておらず、対象は normal route のみで、`chat_analysis.php / chat_advanced.php / AdvancedRouteFinalizer.php` への展開は未実施。runtime では `ProjectMemoryAutoUpdater の overcapture を直してください。` で `command / code_improvement / implementation_plan` の instruction と、現状課題 / 修正方針 / 実装ステップ / 検証方法 / 残課題へ寄った final answer を確認した。`質問分解ロジックを改善してください。` でも prompt に `command / code_improvement / implementation_plan` が入ることを確認したが、project 付き normal route が重く final result までは未取得。`この方針は自然ですか？` では `consultation / proposal` と `すぐ実装扱いにせず、方針確認・リスク・次の選択肢を示す` 系 instruction を確認し、回答も妥当性評価 / リスク / 次の一手に寄った。`次はどうしましょうか？` は `status_check` と現在地整理 + 次アクション提案、`それは違います。質問の因数分解に戻りたいです。` は correction 系 realign instruction と指摘受容 / 論点リセット / 因数分解復帰、`戻っても良いでしょうか？` は rollback 意図に沿った元論点復帰を確認した。`[OLLAMA-RAW-PROMPT]` では full JSON が入っておらず、追加は短い 3 行程度で、`priorityChars` はおおむね 266〜295 に収まり prompt 肥大化は見られなかった。`csv化してください` は `data_analysis.csv_export_request` を維持し、normal prompt 改善による CSV 系への悪影響は見えなかった。一方で `この内容を報告書にしてください。` は `advanced_hybrid.doc_extract` に流れており、これは normal prompt 改善の副作用ではなく既存 report route / factorizer 側の残課題として扱う。`この資料の要点` は `advanced_hybrid.doc_extract` に流れたため normal route の検証対象外として整理する。`5a45833 fix(chat): skip document rag for code improvement prompts` では `chat_normal.php::buildRagContext()` に code improvement 用 context policy を追加し、`user_intent=code_improvement` かつ `expected_response=implementation_plan` のときだけ documents/doc_chunks の vector RAG を skip するようにした。該当時は `VectorSearch::search()` を呼ばず、`sourceDocs` に PDF/doc 根拠を積まない。project memory / history / intent instruction は既存どおり利用し、`[RAG-CONTEXT-POLICY] policy=skip_vector_rag | reason=code_improvement_implementation_plan` で skip 理由を観測可能にした。第1段では完全 skip にしており、route / selector / `PromptManager` / `ConversationIntentInterpreter` は変更していない。class-level では `ProjectMemoryAutoUpdater の overcapture を直してください。`、`質問分解ロジックを改善してください。`、`チャットの回答生成ロジックを改善してください。` の各発話で `vector_calls=0`、`source_docs=[]` を確認し、`PDF図面2.pdf` などの PDF/doc 根拠ノイズが `sourceDocs` に入らないことを確認した。回帰確認として `PDF図面2.pdf の内容を教えてください。` では `vector_calls=1`、`sourceDocs` に `PDF図面2.pdf` が入ること、`このCSVを集計して詳細を教えてください。` では `vector_calls=1`、CSV由来 `sourceDocs` が入ること、`csv化してください` と `この内容を報告書にしてください。` は code improvement 条件外のため skip 対象外であることを確認した。`77b7bf9 fix(chat): filter document history for code improvement prompts` では `chat_normal.php` に code improvement / implementation_plan 用の history context policy を追加し、`user_intent=code_improvement` かつ `expected_response=implementation_plan` のときだけ、`buildDialogueContextPrompt()` に入る前の `historySummaryText` を調整するようにした。assistant 行のうち PDF/doc_extract 系らしい履歴だけを除外し、user 行は残し、assistant 行でも PDF/doc_extract 系でなければ残す。kept が空になった場合だけ user-only fallback する構成で、`projectOperatingMemoryPrompt`、route、RAG、`PromptManager` は変更していない。`[HISTORY-CONTEXT-POLICY] policy=filter_doc_extract_assistant_history` で観測可能にした。mixed history として thread 30 相当の runtime では `request_type=command`、`intent=code_improvement`、`expected_response=implementation_plan`、`route=normal_rag`、`[RAG-CONTEXT-POLICY] policy=skip_vector_rag`、`[HISTORY-CONTEXT-POLICY] ... removed_lines=1 | kept_lines=6 | fallback=none` を確認し、`[OLLAMA-RAW-PROMPT]` の `これまでの会話の文脈` から `PDF図面2.pdf` を含む assistant の PDF抽出回答行が消え、user 行と非PDFの CSV集計 assistant 行は残った。clean history として thread 29 相当の runtime / reflection では `removed_lines=0 | kept_lines=3` を確認し、既存の code_improvement 相談の連続性は維持され、assistant の開発計画回答も保持され、過剰に user-only へ落ちていない。回帰確認として `HISTORY-CONTEXT-POLICY` は code_improvement のみで発火し、PDF質問、CSV集計、artifact、report、consultation では発火しないことを確認した。`0efc81e chore(chat): add normal route phase timing logs` では挙動変更なしで phase timing logs を追加し、`chat_normal.php` に `[NORMAL-PHASE-TIMING]`、`ChatEvaluator.php` に `[EVAL-PHASE-TIMING]` を追加した。normal route の `prompt build / generation start-end / judge start-end / rewrite start-end / save start-end / send final / stream close / exception`、evaluator の `evaluateDraft start-end-error / reviseDraftTextOnly start-end-error` を観測可能にした。runtime では `project_id=4`、`thread_id=40`、`ProjectMemoryAutoUpdater の overcapture を直してください。` を `api/chat.php` 経由で確認し、HTTP 200、`event_types=[\"status\",\"result\"]`、final result 到達、`chat_history` 保存 `user=539 / assistant=540` を確認した。assistant は確認質問に着地し、phase timing は `prompt_build_start=1ms`、`prompt_built=2ms (total_chars=497 / system_chars=7370 / project_memory_chars=6025 / history_chars=0 / context_chars=107 / source_docs=0)`、`generation_start=3ms`、`generation_end=78202ms (response_chars=2521 / token_chunks=1305)`、`judge_start=78222ms (reason=long_answer / timeout=90)`、`[EVAL-PHASE-TIMING] evaluate_end=50909ms (http_code=200 / result=reject / evaluation_mode=real / total_score=0)`、`judge_end=129164ms`、`save_start=129196ms`、`save_end=129332ms (history_id=540)`、`send_final=129337ms`、`stream_close=129348ms`。結論として generation 約78秒 + judge 約51秒で合計約129秒となり、client timeout 120秒に近く通常のデバッグクライアントでは先に切れる可能性がある。`sourceDocs=0` / `historyChars=0` でも重いため、documents/doc_chunks RAG や history ではなく、`projectMemoryChars=6025` を含む system / project memory 側と `long_answer` judge が主な次課題と判断した。回帰確認として今回は fresh runtime は code_improvement 1件のみで、既存保存済み route / 履歴から `thread 32: PDF図面2.pdf の要点を教えてください。 -> advanced_hybrid.doc_extract`、`thread 37: このCSVを集計して詳細を教えてください。 -> data_analysis.csv_agg`、`thread 39: この方針は自然ですか？ -> normal_rag.project_memory_consultation` の整合だけを確認し、PDF / CSV / consultation の fresh runtime 回帰は未実施として残した。残りは `advanced_hybrid.doc_extract / analysis / advanced` への展開要否、priority header の長さ調整、通常RAG代表質問の再選定、prompt scope log の重複調整、global mode の軽い fixture / regression 確認、`質問分解ロジックを改善してください。` のような実装依頼で完走まで時間がかかる点、`この方針は改善した方がよいですか？` が consultation なのに `advanced_hybrid.multi_source_advice` へ流れる点、`この内容を報告書にしてください。` が報告書 route ではなく `doc_extract` に流れる点、analysis / advanced へ intent instruction を広げるかの判断、`normal_rag.code_improvement` の route_detail 新設検討、code_improvement でも資料メモや設計 docs だけ許可するかの境界整理、runtime full chat で回答速度・回答品質・PDFノイズ抑制を確認すること、report 系 artifact の route 判定見直し、`projectOperatingMemoryPrompt` 側にはまだ `報告書/PDF` レーンや `PDF図面2.pdf` 参照が残るため軽いバイアスが残ること、`projectOperatingMemoryPrompt` 側の PDF / 報告書レーンを code_improvement 時に弱めるか検討すること、実ログで確認できた主因が generation 約78秒 + judge 約51秒であること、次はどちらを先に削るか判断が必要なこと、`project_memory_chars=6025` が大きいため code_improvement 時の `projectOperatingMemoryPrompt` 短縮を検討すること、`response_chars=2521` で `long_answer` judge が発火しているため `long_answer` policy の code_improvement 例外化を検討すること、client timeout 120秒と server 側 phase 合計129秒の不整合整理、必要なら次段で PDF / CSV / consultation の fresh runtime 回帰確認、`資料メモを開いて...` の前提操作吸収を行う |
| P1 | 実装済み（要改善） | code_improvement / implementation_plan 時だけ normal route の `long_answer` judge threshold を 1200 から 3000 へ引き上げつつ、answer shape も短文化して judge 約51秒の発火と再 timeout を抑制する | `AI_System_Data/src/ChatEvaluationPolicy.php`, `AI_System_Data/src/PromptManager.php`, `AI_System_Data/public/api/chat_normal.php` | `f4ae710 fix(chat): relax long answer judge for code improvement` で `ChatEvaluationPolicy::shouldEvaluateNormalRag()` に `conversation_intent_profile` を渡せるようにし、`user_intent=code_improvement` かつ `expected_response=implementation_plan` のときだけ `long_answer` threshold を `3000` に引き上げた。通常 threshold は従来どおり `1200` を維持し、完全な judge skip ではなく `long_answer` 単独発火だけを抑制する構成にした。`structured_aggregation_question`、`evidence_sensitive_question`、`multi_source_answer`、`output_mode_requires_quality_check`、通常の `long_answer` は維持している。`JUDGE-NORMAL-SKIP` / `JUDGE-NORMAL-START` / `NORMAL-PHASE-TIMING` には `threshold` を載せて観測可能にし、route / selector / prompt / project memory / DB schema は未変更。class-level では code_improvement + implementation_plan で `response_chars=1300 -> judge しない`、`2600 -> judge しない`、`3200 -> reason=long_answer で judge`、通常 normal_rag では `1300 -> 従来どおり long_answer で judge` を確認した。PDF / CSV 系も `PDF図面2.pdf の内容を教えてください。 -> evidence_sensitive_question`、`このCSVを集計して詳細を教えてください。 -> structured_aggregation_question`、複数 source 条件維持を確認済み。runtime では `ProjectMemoryAutoUpdater の overcapture を直してください。` に対し `route=normal_rag`、`intent=code_improvement`、`expected_response=implementation_plan`、`[RAG-CONTEXT-POLICY] policy=skip_vector_rag`、`[PROJECT-MEMORY-CONTEXT-POLICY] policy=code_improvement_short_memory`、`[JUDGE-NORMAL-SKIP] ... reason=code_improvement_long_answer_threshold | responseChars=2616 | threshold=3000` を確認し、`JUDGE-NORMAL-START` は出ず、judge は発火しなかった。`FINAL-ANSWER` と `stream_close` まで到達し、runtime時間は前回代表値 `約129〜169秒` に対し、今回は `route complete: 71.63秒`、Playwright 実行全体も `約73秒` まで短縮した。続いて `project_id=4` の `api/chat.php` fresh runtime 回帰も確認し、確認入力は `PDF図面2.pdf の要点を教えてください。`、`このCSVを集計して詳細を教えてください。`、`この方針は自然ですか？`、`ProjectMemoryAutoUpdater の overcapture を直してください。` の4件。実際には `thread_id` 省略により resolver が既存 `thread_id=40` へ継続したため、完全新規 thread ではない。PDF は `advanced_hybrid.doc_extract` を維持し、subtype `summary`、final answer 見出し `## 要点`、`## 留意点` 固定への逆戻りなし、`[DOC-EXTRACT-TYPE] type=summary` が planner / builder 両方で出て、保存は `user=551 / assistant=552`。CSV は `data_analysis.csv_agg.route_lock` を維持し、PDF doc_extract には流れず、`AI集計表_20260608_195156_395.csv` の value distribution 回答を返し、保存は `user=553 / assistant=554`。consultation は期待 `normal_rag.project_memory_consultation` に対して実際は `advanced_hybrid.multi_source_advice` へ流れ、回答内容自体は相談回答だったが route 選択は期待と異なり、保存は `user=555 / assistant=556`。これは別件の残課題として扱う。code_improvement は `route=normal_rag`、`intent=code_improvement`、`expected_response=implementation_plan`、`[RAG-CONTEXT-POLICY] policy=skip_vector_rag`、`[PROJECT-MEMORY-CONTEXT-POLICY] policy=code_improvement_short_memory`、`response_chars=2809`、`threshold=3000`、`[JUDGE-NORMAL-SKIP] reason=code_improvement_long_answer_threshold` を確認し、`JUDGE-NORMAL-START` は出ず、final result まで到達、保存は `user=557 / assistant=558`。runtime / 応答時間は code_improvement で `generation_end=67770ms`、`send_final / stream_close=67795ms` で 120秒以内に収まり、前回の 129〜169秒より改善した。さらに `4741337 fix(chat): shorten code improvement answer shape` では `PromptManager.php` と `chat_normal.php` の最小変更だけで、`user_intent === 'code_improvement'` かつ `expected_response === 'implementation_plan'` のときだけ answer shape を短文化した。目安は `1800〜2400 chars`、見出しは `方針 / 最小変更 / 確認 / 残課題` の最大4つ、各見出しは `2〜4項目` に抑え、コード全文、最終報告テンプレ、長い背景説明、過度な一般論を抑止する。`long_answer` threshold は `3000` のまま維持し、judge 完全 skip、route新設、selector変更、RAG policy変更、DB schema / DB更新はしていない。class-level では `ProjectMemoryAutoUpdater の overcapture を直してください。`、`質問分解ロジックを改善してください。`、`チャットの回答生成ロジックを改善してください。` を対象に、`command / code_improvement / implementation_plan / execute_requested_task` を維持したまま、`PromptManager::buildResponsePriorityHeader()` に短文化された回答方針 / 出力制約 / 冗長化防止が入り、`chat_normal.php` 側でも `1800〜2400`、見出し最大4つ、最小差分 / 確認方法 / 残課題を含む system prompt が入ることを確認した。runtime の main case `ProjectMemoryAutoUpdater の overcapture を直してください。` では `thread_id=51`、`route=normal_rag`、`intent=code_improvement`、`expected_response=implementation_plan`、`[RAG-CONTEXT-POLICY] policy=skip_vector_rag`、`[PROJECT-MEMORY-CONTEXT-POLICY] policy=code_improvement_short_memory`、`response_chars=2038`、`[JUDGE-NORMAL-SKIP] reason=code_improvement_long_answer_threshold | responseChars=2038 | threshold=3000` を確認し、`JUDGE-NORMAL-START` は出ていない。`total runtime=50.445207s`、`generation_end=50344ms`、`stream_close=50390ms` で final result まで到達し、保存は `user=599 / assistant=600`、`save_end history_id=600` を確認した。回帰確認として `PDF図面2.pdf の要点を教えてください。 -> advanced_hybrid.doc_extract / type=summary / ## 要点`、`このCSVを集計して詳細を教えてください。 -> data_analysis.csv_agg`、`この方針は自然ですか？ -> normal_rag.project_memory_consultation` を runtime で維持確認済み。結論として、再 timeout の主因は RAG / history / project memory ではなく implementation_plan の回答形が長すぎることだった。answer shape 短文化により、前回 `response_chars=3292 / timeout` だった main case が `response_chars=2038 / 50.45s 完走` まで改善し、`long_answer threshold=3000` は維持したまま judge を踏みにくくできた。PDF / CSV / consultation への波及は今回見えていない。残りは response_chars が安定して `3000` 未満に収まるかの継続確認、具体性が落ちすぎていないかの確認、冒頭説明がまだ厚い場合の `背景2文まで` 追加検討、`normal_rag.code_improvement` 専用 policy 化の要否判断、`thread_id` 省略時に既存 thread へ継続される挙動の整理、client timeout `120秒` との整合整理を行う。 |
| P1 | 実装済み（要改善） | `route confidence` 第1段として `ChatRouteFactorizer.php` / `ChatRouteSelector.php` / `chat.php` に `route_confidence / reason_codes / evidence` を追加し、`[SMART-ROUTER-CONFIDENCE]` ログで route 決定根拠を可視化する | `AI_System_Data/src/ChatRouteFactorizer.php`, `AI_System_Data/src/ChatRouteSelector.php`, `AI_System_Data/public/api/chat.php` | 第1段では route 挙動、APIレスポンス、DBは変更せず、ログ可視化だけを追加した。`analysis recommendation: medium / advanced_hybrid.multi_source_advice`、`project status: high / normal_rag.project_memory_consultation`、`material workflow: high / normal_rag.project_memory_consultation`、`history report: high / advanced_hybrid.history_report`、`csv aggregation: factorized high / selected medium / data_analysis.csv_agg / target_column=い再発なし`、`document extract: medium / advanced_hybrid.doc_extract` の6ケースで確認済み。結論として、`medium` は「広め・曖昧めの根拠で選ばれた route」であり、現時点では route 挙動変更は行わず、残りは `selected_confidence` 算出ルールの微調整、`medium` かつ回答品質が悪い実例の収集、必要時の fallback 設計を行う |
| P1 | 実装済み（要改善） | PDF質問が常に「留意点」回答へ寄りすぎる問題の第1段として、`advanced_hybrid.doc_extract` route は維持したまま、質問タイプ別に `AdvancedDocAnswerBuilder` の prompt / 出力フォーマットを分岐する | `AI_System_Data/src/AdvancedDocAnswerBuilder.php`, `AI_System_Data/src/AdvancedRoutePlanner.php` | `20f456b improve(chat): vary pdf extraction answers by question type` で `advanced_hybrid.doc_extract` route 自体は維持したまま、`AdvancedDocAnswerBuilder` に PDF質問 subtype 判定を追加した。subtype は `summary / explanation / cautions / report_materials` の4種で、`AdvancedDocAnswerBuilder` の system prompt / output rules / deterministic answer を subtype 別に分岐した。`留意点` 質問だけ既存の `## 留意点` 形式を維持し、`内容説明 / 要点 / 報告書材料` では `## 留意点` 固定を外した。さらに `AdvancedRoutePlanner::buildPresetDocPlan()` の purpose も subtype に追従するようにし、route / selector / factorizer の大改修はまだ行っていない。`[DOC-EXTRACT-TYPE] type=... | source=builder/planner` で観測可能にした。class-level の PDF質問 fixture では `PDF図面2.pdf の内容を教えてください。 -> explanation / ## 内容説明`、`PDF図面2.pdf の要点を教えてください。 -> summary / ## 要点`、`PDF図面2.pdf の留意点を教えてください。 -> cautions / ## 留意点`、`このPDFから報告書に使える材料を整理してください。 -> report_materials / ## 報告書材料`、`このPDFの図面内容を説明してください。 -> explanation / ## 内容説明` を確認し、`留意点` 質問の既存回答を維持できることも確認済み。`AdvancedRoutePlanner::buildPresetDocPlan()` の purpose も subtype に追従することを class-level で確認した。`git diff --check` OK、Docker内 `php -l` で `AI_System_Data/src/AdvancedDocAnswerBuilder.php` と `AI_System_Data/src/AdvancedRoutePlanner.php` は OK。runtime full chat では `project_id=4`、`api/chat.php` 経由、PDF質問に `thread_id=31,32,33,34,35`、回帰確認に `thread_id=36,37,38,39` を使用して確認済み。`PDF図面2.pdf の内容を教えてください。` は `advanced_hybrid.doc_extract / explanation / ## 内容説明`、`PDF図面2.pdf の要点を教えてください。` は `advanced_hybrid.doc_extract / summary / ## 要点`、`PDF図面2.pdf の留意点を教えてください。` は `advanced_hybrid.doc_extract / cautions / ## 留意点` で既存パターン維持、`このPDFから報告書に使える材料を整理してください。` は `advanced_hybrid.doc_extract / report_materials / ## 報告書材料`、`このPDFの図面内容を説明してください。` は `advanced_hybrid.doc_extract / explanation / ## 内容説明` を確認した。5件すべてで planner / builder の両方に `[DOC-EXTRACT-TYPE]` が出ており、確認できた subtype は `explanation / summary / cautions / report_materials`。回帰確認として `csv化してください` は `data_analysis.csv_export_request` で PDF doc_extract に流れず、`このCSVを集計して詳細を教えてください。` は `data_analysis.csv_agg` を維持し、`ProjectMemoryAutoUpdater の overcapture を直してください。` は `intent=code_improvement / route=normal_rag / [RAG-CONTEXT-POLICY] policy=skip_vector_rag` で PDF doc_extract への副作用がなく、`この方針は自然ですか？` も `normal_rag.project_memory_consultation` を維持した。応答時間は `内容説明=約21.24秒 / 要点=約10.76秒 / 留意点=約10.82秒 / 報告書材料=約11.01秒 / 図面内容説明=約11.38秒` で5件とも完走した。一方で `ProjectMemoryAutoUpdater の overcapture を直してください。` は今回も normal route 側で 120 秒 timeout し final answer 未取得だったため、これは PDF subtype とは別の既知課題として normal route / code_improvement 系の重さ調査へ回す。残りは `route_detail` を `doc_extract.summary / explanation / cautions / report_materials` に細分化するかの判断、`PDF QA / specific question` 型の追加検討、code_improvement / normal route の timeout・重さ調査、report 系 artifact route 判定見直し、`SSE timeout / final answer phase timing` 調査、`projectOperatingMemoryPrompt` の code_improvement バイアス調査を行う |
| P1 | 実装済み（要改善） | FAQ auto-registration の第一段として `FaqAutoRegistrar.php` に登録直前ガードを追加し、FAQ 登録してはいけない回答を自動登録対象から外す | `AI_System_Data/src/FaqAutoRegistrar.php` | `verdict_not_pass` / `mismatch_pair` / `clarification_marker` / `rewrite_marker` / `insufficient_evidence_text` / `operation_notice_text` で `[FAQ-AUTO]` が skip されること、実運用では FAQ 登録ログの確認、誤登録/登録漏れの検証、必要に応じた判定文言の調整を続ける |
| P1 | 実装済み（要改善） | 回答直前の適合性ガード第1段として `LightweightFinalAnswerGuard.php` に deterministic rule を追加し、操作説明だけの回答、根拠不足定型文、`相談 / 資料メモ更新 / 報告書化` の代表的な誤脱線を出荷前に止める | `AI_System_Data/src/LightweightFinalAnswerGuard.php` | `FINAL-GUARD-OPERATION-NOTICE` / `FINAL-GUARD-INSUFFICIENT-EVIDENCE` / `FINAL-GUARD-INTENT-DRIFT` が機能すること、`project_memory_consultation` / `history_report` / `data_analysis.csv_agg` の正常系が過剰ブロックされないことを確認済み。残りは実運用ログで「確認質問に倒したいのに倒せなかった実例」を収集し、必要なら `ClarificationQuestionBuilder.php` を拡張する |
| P2 | 実装済み（要改善） | 抽象的な分析相談を metadata / advisory 寄りへ寄せる第1段・第2段として、`advanced_hybrid.multi_source_advice` の advisory 材料と入口相談ルーティングを強化する | `AI_System_Data/src/AdvancedFastPathResolver.php`, `AI_System_Data/src/ChatRouteFactorizer.php` | `AdvancedFastPathResolver.php` の `advanced_hybrid.multi_source_advice` を改善し、CSV / PDF 一覧に加えて `project memory`、資料メモ、短い recent context を advisory 材料として使うようにした。回答構造は `まず見るべきもの / CSVで確認する観点 / PDF/資料メモで確認する観点 / 次に実行するとよい具体アクション` に整理し、資料メモの重複表示を抑制して回答長も圧縮した。さらに `ChatRouteFactorizer.php` に入口相談語彙を追加し、`この案件で、まずどこから見ればよいですか。` 系が `advanced_hybrid.multi_source_advice` に入るようにした。確認済み: `どのように分析したらよいでしょうか。` は `advanced_hybrid.multi_source_advice` のまま SQL/実集計に入らず advisory 回答になる、`この案件で、まずどこから見ればよいですか。` は `advanced_hybrid.multi_source_advice` に入る、`出荷一覧表を月別に集計してください。` は `data_analysis.csv_agg` のまま、`この資料の要点を教えてください。` は `advanced_hybrid.doc_extract` のまま、`現在の進行中タスクは？` は `normal_rag.project_memory_consultation` のまま。残りは advisory 回答の長さ・粒度の実運用調整、`analysis_recommendation_request` の語彙過剰反応の継続確認、medium confidence の advisory route で回答品質が悪い実例が出た場合の fallback 設計、必要に応じた `selected_confidence` 算出ルールの微調整を行う |
| P1 | 実装済み（要改善） | 会話履歴から `成果品ステート` を復元し、CSV follow-up の route lock と履歴の報告書化優先を実装する | `AI_System_Data/src/ChatHistoryContextResolver.php`, `AI_System_Data/src/ChatRouteSelector.php`, `AI_System_Data/public/api/chat.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/public/api/chat_history_summary.php` | `若い順で`, `グラフ化して`, `これまでの会話を報告書にして` が期待 route に入り、`route_detail_override` と着地が実ログで安定することを確認する |
| P2 | 実装済み（要改善） | `chat_analysis.php` の planner / runner / prompt に復元した `成果品ステート` を渡し、route lock 後の出力継続を決定論的にする | `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/src/CsvAggregationPlanner.php`, `AI_System_Data/src/CsvDateAggregationRunner.php`, `AI_System_Data/src/CsvValueAggregationRunner.php`, `AI_System_Data/src/CsvAggregationAnswerFormatter.php` | `output_format` / `chart_type` / `base_sql` を保持した follow-up が崩れないことを確認する |
| P2 | 実装済み（要改善） | `main / sub / sql / embedding / vision` の5層モデル責務を route 全体へ適用する | `AI_System_Data/src/ModelRoleResolver.php`, `AI_System_Data/public/api/save_user_settings.php`, `AI_System_Data/public/api/chat.php`, `AI_System_Data/public/api/upload.php` | `result.model_roles` と `[MODEL-ROLES]` ログ、`[PDF-IMAGE-MODEL] vision_model=...` の出力を route ごとに確認する |
| P2 | 実装済み（要改善） | 単発・軽量ルートの最終回答ガードを rule-first に寄せ、deterministic 出力を壊さずに質問適合性を確認する | `AI_System_Data/src/LightweightFinalAnswerGuard.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/public/api/chat_history_summary.php` | `json:chart` や Mermaid が rewrite で壊れず、資料PDF質問で根拠寄り回答になることを確認する |
| P2 | 実装済み（要改善） | 報告書モードの本文精度を route 横断で揃えるため、通常ルートとデータ分析ルートでも報告書向け最終整形を共通化する | `AI_System_Data/src/ReportAnswerPolisher.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/src/ReportGenerator.php` | `report_mode=on` で `## 結論 / ## 分析対象 / ## 根拠 / ## 留意点 / ## 推奨アクション / ## 出典` の骨格へ寄ることを確認する |
| P2 | 実装済み（要改善） | 文章改善・提案相談で、CSV名言及だけで `data_analysis` に倒れないよう route 競合と RAG コンテキスト汚染を抑える | `AI_System_Data/src/ChatRouteFactorizer.php`, `AI_System_Data/src/ChatRouteSelector.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/src/RAGPipeline.php` | 相談系で CSV 証拠読解へ落ちず、`[RAG-CONTEXT-FILTER]` と `最終ルート決定` が期待どおりになることを確認する |
| P3 | 実装済み（要改善） | 全社横断ブリーフィング系の route・ReAct・host timeout フォールバックの安定化 | `AI_System_Data/public/api/chat_global.php`, `AI_System_Data/public/api/chat.php`, `AI_System_Data/src/ChatRouteDispatcher.php`, `AI_System_Data/src/EmbeddingEngine.php` | `GLOBAL-REACT-STOP` / `GLOBAL-MODEL-FALLBACK` と最終回答が数十秒以内に着地することを確認する |

## 検証中

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P2 | 検証中 | `chat_advanced.php` の責務分離候補を棚卸しし、分割順序を確定する | `AI_System_Data/public/api/chat_advanced.php`, `AI_System_Data/src/AdvancedDocAnswerBuilder.php`, `AI_System_Data/src/AdvancedSubQueryNormalizer.php`, `AI_System_Data/src/AdvancedRoutePlanner.php`, `AI_System_Data/src/AdvancedPlanExecutor.php`, `AI_System_Data/src/AdvancedDraftComposer.php`, `AI_System_Data/src/AdvancedCriticLoop.php`, `AI_System_Data/src/AdvancedRouteFinalizer.php` | `advanced_hybrid.doc_extract` で既存ログと最終回答が崩れないことを確認する |
| P2 | 検証中 | 本番環境の PDF プレビューで `documents.file_path` と実ファイル配置のズレを吸収し、`view_pdf.php` の安定配信を確認する | `AI_System_Data/public/api/view_pdf.php`, `AI_System_Data/public/api/delete_pdf.php`, `AI_System_Data/public/support.php`, `AI_System_Data/public/assets/js/modules/ui.js` | `pdf_view.log` の `stream started` 到達と、Chrome / Edge 埋め込みプレビュー成功を確認する |

## 保留 / 再導入検討

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P3 | 保留 | watchdog 常駐監視を現行構成へ戻すか判断し、必要なら実装・Docker・README・運用手順をまとめて再導入する | `docker-compose.yml`, `README_01.md`, `README_DOCKER.md`, `AI_System_Data/public/docs/design_v3.html`, `scripts/watchdog.php` | 方針決定までは現行構成から外す。再導入する場合は、実ファイル配置、起動方法、ログ出力、監視対象パスが揃うことを確認する |

## 要確認

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P3 | 要確認 | `PromptManager.php` を中心とした prompt 組み立て責務の再配置範囲をどこまで広げるか | `AI_System_Data/src/PromptManager.php`, `AI_System_Data/public/api/chat_normal.php`, `AI_System_Data/public/api/chat_analysis.php`, `AI_System_Data/public/api/chat_advanced.php` | `PROMPT-BUDGET` ログと route ごとの差分を見て、共通化が有効な範囲を見極める |

## 完了

| 優先度 | 状態 | タスク | 対象ファイル | 確認方法 |
| --- | --- | --- | --- | --- |
| P1 | 完了 | `chat_threads` と `chat_history.thread_id` を正式スキーマへ反映し、README / 設計書の説明も現行実装へ追従させる | `AI_System_Data/config/db.sql`, `AI_System_Data/config/schema_check.sql`, `README_01.md`, `README_DOCKER.md`, `AI_System_Data/public/docs/design_v3.html` | `db.sql` / `schema_check.sql` / README / 設計書で thread 構成、`view_pdf.php`、モデル既定値、watchdog 現状が整合していることを確認した |
